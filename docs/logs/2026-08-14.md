## Performance

**Tool:** SigNoz
**Период:** заявлено 2026-08-13 06:00 → 2026-08-14 06:00 UTC — фактических данных за этот период **нет** (см. ниже)

### Key Metrics

| Metric | Value | vs вчера |
|---|---|---|
| Запросов за последние 24ч | **0** | 54331 |
| Ошибок | — (нет данных) | 21 (0.04%) |
| Медленные DB-запросы (>100мс) | — (нет данных) | 0 |

### Findings

- **Полный обрыв телеметрии**: SigNoz не получал ни трейса от сервиса `scopegate` с 2026-08-12 ~16:00 UTC — 38+ часов на момент проверки. Бинарный поиск по окнам (1ч…62ч) подтверждает: любое окно, кончающееся позже 2026-08-12 17:00 UTC, даёт 0 запросов; окно, включающее более раннее время, снова видит трафик (105226 запросов за 2026-08-11 16:00 → 2026-08-12 17:00 UTC). Сайт при этом жив (`curl` → 200 OK) — обрыв только в телеметрии. Совпадает по времени с редеплоем на новый `docker-compose.yaml` (f4b0468, добавил `OTEL_EXPORTER_OTLP_ENDPOINT` в шаблон, но не гарантирует, что ключ выставлен в самом Coolify-ресурсе; `src/instrumentation.node.ts:167-171` молча выключает трейсинг, если переменная пуста). Все три прод-фикса за 08-13 (SSE 204-краш, GSC/LinkedIn таймауты) задеплоены уже во время обрыва — их работу в проде подтвердить нечем. → задача #231 (`critical`, требует проверки env на Coolify-ресурсе — не автоисполнима).

### Recommendations

1. Проверить и восстановить `OTEL_EXPORTER_OTLP_ENDPOINT`/`OTEL_SERVICE_NAME` на Coolify-ресурсе scopegate.dev → задача #231.
2. После восстановления — разово перепроверить задачи #224/#225/#226 по свежим трейсам.

## Changes

- Task #231 — `??` → `||` для `OTEL_SERVICE_NAME` в `src/instrumentation.node.ts` (пустая строка из `${OTEL_SERVICE_NAME:-}` больше не проглатывает fallback-имя); фикс существовал на висящей ветке `worktree-issue-231`, смержен fast-forward в master (39d9f20)
