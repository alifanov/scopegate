# 2026-08-23

## Security

**Period:** 2026-08-16 — 2026-08-23 (с прошлого чистого прогона security-audit)

### Findings

| Category | Finding | Severity | File / Config | Source |
|---|---|---|---|---|
| DoS / rate-limit bypass | `invalidKeyBuckets` не чистится и ключуется по подделываемому первому хопу `X-Forwarded-For`: обход брутфорс-лимитера + рост памяти без границ (200k ключей ≈ 46 МБ, не освобождается) | high | `src/lib/mcp/api-keys.ts:13,19-30,32-42` | code-review |
| Vulnerable dependency | `deepmerge-ts@7.1.5` < 8.0.0 — stack exhaustion на рекурсивном графе; транзитивно через `prisma → @prisma/config`, управляемый атакующим граф туда не доходит | high | `pnpm-lock.yaml` (GHSA-ggr8-5vv4-36mx) | github |
| Robustness (low, задача не заводилась) | `parseInt` по `page`/`limit` даёт `NaN` → Prisma бросает → 500 | low | `src/app/api/projects/[projectId]/audit/route.ts:19-20` | code-review |

Code scanning и secret scanning на репозитории выключены (404) — с этих поверхностей находок нет.

### Recurring Issues

Нет. Обе находки новые; повторов с прошлых аудитов не осталось — SSRF/DNS-rebinding, path traversal в путях MCP-инструментов (#188) и SSRF через imapHost/smtpHost (#190) перепроверены, фиксы на месте и не регрессировали.

### Recommendations

1. **Память лимитера MCP-ключей** → чистить истёкшие бакеты и ограничивать размер Map (как `pruneMemoryStore` в better-auth, потолок 1e5), брать последний хоп `X-Forwarded-For` вместо первого → тест: 200k уникальных IP оставляют в Map не больше потолка; `getClientIp()` на `1.1.1.1, 203.0.113.7` возвращает адрес от прокси. Задача #275.
2. **deepmerge-ts** → `pnpm.overrides` на `^8.0.0`, `pnpm install` → тест: `pnpm why deepmerge-ts` не показывает 7.x, CI зелёный, alert #170 закрыт. Задача #276.

## Changes

- Task #275 — invalidKeyBuckets: ограничен потолком в 20k записей + чистка протухших бакетов; getClientIp() теперь берёт x-real-ip / последний хоп X-Forwarded-For вместо первого (src/lib/mcp/api-keys.ts)
- Task #276 — deepmerge-ts закреплён на ^8.0.0 через pnpm.overrides, закрывает GHSA-ggr8-5vv4-36mx (ae4ac35)
- Task #259 — FAB почты возвращён в угол (`bottom-5`, убран `bottom-24`/`sm:bottom-5`), больше не перекрывает контент маркетинговых страниц на мобильных; регрессия #242 закрыта отступом снизу у последней секции `/pricing` (`pb-24` на мобильных) вместо переноса кнопки (src/app/layout.tsx, src/app/pricing/page.tsx)
- Task #278 — scopegate.dev 503 «no available server»: деплой #12279 (commit aa3acb0, 11:01 UTC) упал на шаге `docker compose up` — `cron`-сервис не был собран (`No such image: ..._cron:aa3acb0...`), приложение осталось без здорового бэкенда за Traefik. Ретрай того же коммита (деплой #12281, 11:15 UTC) прошёл штатно, сайт вернул 200 к 13:16 UTC. Код и compose-файл не менялись — одноразовый сбой билд-пайплайна Coolify, не воспроизводится на идентичном конфиге
- Task #260 — CLS на `/` (hero): локальный прогон (mobile 390×844, dev-сборка) уже даёт 0 layout-shift'ов — фикс #244 (все строки код-блока рендерятся сразу под `invisible`) закрыл рост высоты. Скорее всего задача была заведена по устаревшему прод-деплою (сайт как раз отдавал 503, см. #278, деплой пайплайна ломался). Независимо от причины применён защитный пункт acceptance criteria: `items-center` → `items-start lg:items-center` в `src/components/landing/hero.tsx:77`, чтобы будущий поздний рост высоты секции не перецентровывал весь hero
- Task #261 — /pricing показывал заголовок «Simple, transparent pricing» дважды (hero страницы + `<Pricing />`): компонент получил проп `showHeading` (по умолчанию `true`), `/pricing` передаёт `showHeading={false}` — на лендинге (`/`) заголовок секции не изменился (src/components/landing/pricing.tsx, src/app/pricing/page.tsx)
- Task #266 — кнопка подключения в диалоге «Connect a Service» уезжала за край на 390 и 1440 px: таблица переведена на `table-fixed` с колонками Service (усечение длинных имён через `truncate`) / Auth (80px→96px) / Connect (56px), колонка Description убрана (описание доступно через `title` на строке), кнопка получила `aria-label`, добавлено поле поиска по 27 сервисам (src/components/project/services-tab.tsx)
- Task #267 — годовая цена с `/pricing` теперь покупаема: `PLAN_REGISTRY` получил `polarProductIdEnvYearly` (POLAR_PRODUCT_{PRO,TEAM}_YEARLY), `auth.ts` регистрирует отдельный Polar checkout slug на каждый (план, период) через новый `checkoutSlug()`, `/billing` получил тумблер Monthly/Annual с ценами $23/$119, `/signup?billing=annual` пробрасывается через `CloudSignIn.callbackURL` на `/billing?billing=annual` с уже выбранным периодом (src/lib/plans.ts, src/lib/auth.ts, src/app/(dashboard)/billing/{page,billing-client}.tsx, src/app/(auth)/signup/page.tsx, src/components/auth/cloud-sign-in.tsx)
- Task #268 — пустой проект больше не упирался в тупик: вкладка Endpoints при 0 подключений показывает главный CTA «Connect a service» (→ `?tab=services`), «New Endpoint» становится вторичной/disabled; диалог «Create MCP Endpoint» без сервисов получил кнопку «Connect a service» вместо только крестика (src/components/project/endpoints-tab.tsx, src/components/project/create-endpoint-dialog.tsx)
- Task #262 — демо-видео на `/` больше не грузится при заходе на страницу: `preload="none"` + `poster="/scopegate-demo-poster.webp"` + `aspect-video` (без прыжка разметки до клика по плееру), `/scopegate-demo.mp4` и постер отдаются с `Cache-Control: public, max-age=31536000, immutable` (src/components/landing/demo-video.tsx, next.config.ts, public/scopegate-demo-poster.webp)
- Task #263 — заголовок hero на `/` больше не обрезается на 320 px: левой колонке грида добавлен `min-w-0` (та же причина, что в #241), `h1` получил мобильную ступень `text-4xl sm:text-5xl xl:text-6xl` (src/components/landing/hero.tsx)
- Task #264 — cyan-акцент на /docs заменён на фиолетовый (2f03200)
- Task #265 — кликабельная область соцссылок GitHub/X в футере увеличена до 36×36 (`size-9 -m-1.5`, WCAG 2.5.8), визуальный размер иконок (18/16px) не менялся (src/components/landing/footer.tsx)
