# Observability Review — 2026-07-15

**Tool:** SigNoz (self-hosted, `signoz.chatindex.app`)
**Период:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs 2026-07-14 |
|---|---|---|
| Error rate | 2 ошибки (0.01%) | ↓ было 6 (0.01%) |
| p99 латентность (топ, без SSE-эндпоинтов) | 7 256 мс (threads_publish_thread) | ↓ было 9 501 мс, в рамках 25s budget |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ стабильно (было 0) |
| Объём запросов (спанов) | 22 694 | 🟡 ↓ было 46 236 (−51%) |

## Findings

### Ошибки

| Маршрут | Статус | Кол-во | Комментарий |
|---|---|---|---|
| `/api/mcp/[apiKey]` | 401 | 1 | Неверный/просроченный API-ключ — ожидаемо для внешнего клиента |
| `/api/auth/[...all]` | 400 | 2 | Некорректный запрос к auth — единично, ниже порога |

Логов уровня ERROR/FATAL за период нет.

### Латентность

| Эндпоинт | p99 | Оценка |
|---|---|---|
| `GET /api/mcp/[apiKey]` | ~70.3 мин | ✅ SSE keep-alive — ожидаемо |
| `GET` (orphaned span) | ~60.6 мин | 🟡 2-й снапшот подряд в том же диапазоне (было ~62.9 мин на 07-14) — см. Recurring Issues |
| `mcp.tool threads_publish_thread` | 7 256 мс | 🟢 Улучшение vs 9 501 мс, в рамках 25s budget |
| `service-fetch threads` | 3 583.8 мс | 🟢 Ожидаемо для Threads async API |
| `mcp.tool googleAds_get_ad_group_performance` | 941 мс | 🟢 |
| `mcp.tool googleAds_list_recommendations` | 904 мс | 🟢 |
| `mcp.tool googleAds_list_keywords` | 609.4 мс | 🟢 |
| `POST /api/mcp/[apiKey]` | 601.1 мс | 🟢 |
| `mcp.tool gmail_search_emails` | 599.5 мс | 🟢 |
| `mcp.tool googleAds_list_ad_groups` | 575.9 мс | 🟢 |

### База данных

Все запросы в топ-5 < 35 мс — здоровое состояние.

| Запрос | p99 | Оценка |
|---|---|---|
| `SELECT pg_advisory_xact_lock(...)` | 34.6 мс | ✅ (ожидаемая сериализация `withConnectionLock`) |
| `SELECT ServiceConnection` | 31.6 мс | ✅ |
| `UPDATE ServiceConnection` | 23.8 мс | ✅ |
| `SELECT McpEndpoint` | 19.1 мс | ✅ |
| `SELECT McpEndpoint` | 16.1 мс | ✅ |

### Объём запросов

22 694 спана за 24 ч — заметное падение относительно 07-14 (46 236, −51%). Единственная точка сравнения после 13-дневного разрыва в снапшотах (07-01 → 07-14 были ≈ стабильны: 47 361 → 46 236), поэтому пока нельзя отличить нормальную суточную вариацию от реального падения трафика. См. Hypotheses.

### Интеграции

Threads, Google Ads, Gmail — репортируют спаны стабильно. Пробелов в трейсинге не обнаружено.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| `GET` (orphaned span, вероятный SSE-артефакт) | 2/3 после разрыва (07-14: ~62.9 мин, 07-15: ~60.6 мин) | Подтверждается — если завтра (07-16) снова в этом диапазоне, завести issue на исследование причины |

## Hypotheses

- **Объём запросов упал на 51% (46 236 → 22 694).** Один снапшот сравнения, предыдущая пара точек (07-01/07-14) была стабильна. Может быть нормальной суточной/недельной вариацией использования интеграций, а не деградацией. Status: 1/3 снапшотов — наблюдать в следующих 1-2 ежедневных прогонах; если тренд подтвердится (устойчивое снижение) — завести issue на расследование причины (например, отвал одного из клиентов/интеграций).
- **`GET` orphaned span p99 ~60.6 мин** — см. Recurring Issues, 2/3 подтверждений, решение по issue — после снапшота 07-16.

## Recommendations

1. Без новых issue — ни одна находка не достигла порога `medium`+ по критичности за один снапшот.
2. Завтра (07-16) — решить по orphaned `GET` span: если p99 снова в диапазоне ~60 мин, завести issue на исследование причины (возможно, стоит явно именовать/группировать этот span вместо "orphaned").
3. Продолжить наблюдение за падением объёма трафика (−51%) в следующем снапшоте — при подтверждении тренда завести issue.

## Новые GitHub Issues

Нет — ни одна находка не достигла порога `medium`+ по критичности.
