# 2026-08-10

## Логи (SigNoz)

Окно: 2026-08-09 06:00 → 2026-08-10 06:00 UTC · спанов 61 942 · ошибок 6 (0.01%).
Предыдущего лога с этой секцией нет — сравнения Δ нет, это базовая точка.

**Лог-записи (severity ERROR/WARN): 0.** Приложение шлёт в SigNoz только трейсы, логов от `service.name=scopegate` нет вообще (ни одного уровня). Разбор ниже — по error-спанам.

### Error-спаны

| Спан | statusMessage | Count | Оценка |
|---|---|---|---|
| `service-fetch searchConsole` → `POST` | Request timed out after 5000ms | 2 (+2 родительских) | разовый всплеск 23:27:45 и 23:27:56, две попытки подряд |
| `refresh_connection` | YouTube token refresh failed (400) | 1 | штатный permanent OAuth-error → revoke + нотификация |
| `refresh_connection` | Gmail token refresh failed (400) | 1 | то же |

### HTTP 4xx/5xx

| Маршрут | Статус | Count |
|---|---|---|
| `/login/..;/actuator/env` | 404 | 1 |
| (без маршрута) | 404 | 1 |

**5xx — нет.** 404 — сканеры-пробники, не наш трафик.

### Выводы

- Ошибок уровня приложения нет: 6 error-спанов на 61 942 запроса, все объяснимы (2 таймаута GSC + 2 мёртвых OAuth-токена).
- Слепая зона: без logs-пайплайна проверка видит только spans — `console.error` в проде нигде не наблюдается.
- Порог «наблюдение → задача» по таймаутам GSC не пройден (разовый всплеск) — вернуться, если повторится 3 дня подряд.

### Задачи

- «Приложение не шлёт логи в SigNoz — за 24ч 0 записей от service.name=scopegate» → `needs_human` (предложение, решает владелец).

## Изменения

- «Приложение не шлёт логи в SigNoz» — добавлен OTel logs-пайплайн (`BatchLogRecordProcessor` + `OTLPLogExporter` → `/v1/logs`) и мост `console.error/warn/info` → log records в `src/lib/otel-console-bridge.ts`. Пуш в master напрямую (стратегия проекта — без PR), коммит `f3961ef`, CI зелёный.
