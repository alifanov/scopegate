# 2026-09-01

## Architecture

**Scope:** границы модулей, связность и слои по всему `src/` вручную; TS/JS-половина — fallow 2.104.0 (`audit`, `dead-code`, `dupes`, `health --hotspots --targets`), 334 файла / 41 264 LOC.

### Summary

| Category | Found | Filed | Notes |
|---|---|---|---|
| Structure / boundaries | 3 | 3 | дрейф списка Google-провайдеров; мёртвый аллоулист в middleware; серверный реестр в клиентском бандле |
| Dead code | 26 | 0 | 25 unused exports — все vendored shadcn/ui + `docker/prisma.config.ts` (рантайм-конфиг для `migrate deploy`) |
| Duplication | 6.0% | 1 | 77 clone-групп; отфильтровано до одного шва — «ApiError → toast» ×34 в 18 файлах |
| Circular deps | 0 | 0 | — |
| Complexity hotspots | 70 функций выше порога | 1 | `ServicesTab` cognitive 77 при пороге 30 и churn 27 коммитов |
| Dependency hygiene | 0 | 0 | ни одной неиспользуемой/незаявленной зависимости |

### Filed tasks

| Finding | File(s) | Priority | Confidence | Task |
|---|---|---|---|---|
| `GOOGLE_PROVIDERS` разъехался с реестром: `youtube` и `googleTagManager` не отзываются у Google при disconnect | `src/lib/service-revocation.ts:4`, `src/lib/__tests__/service-revocation.test.ts:9` | high | high | #292 |
| `isPublicPath` не влияет на поведение — обе ветки возвращают `next()` | `src/middleware.ts:30-56` | medium | high | #293 |
| God-компонент: 735-строчная функция, 24 `useState`, 4 формы в одном диалоге | `src/components/project/services-tab.tsx` | medium | high | #294 |
| Весь `PROVIDER_REGISTRY` (1159 строк, серверные transport/token/env) попадает в клиентский бандл | `src/lib/provider-registry.ts`, `src/components/project/services-tab.tsx:36`, `src/components/project/permission-picker.tsx:5` | medium | high | #295 |
| Транспорт есть, шва над ним нет: `ApiError → toast` скопирован 34 раза в 18 файлах | `src/lib/api-client.ts` + 18 клиентских файлов | medium | high | #296 |

### Skipped (false positives / low confidence)

- **25 unused exports** — 24 из них vendored shadcn/ui (`badge`, `button`, `card`, `dialog`, `dropdown-menu`, `table`, `tabs`): публичная поверхность вендоренного компонента, удаление ломает следующий `shadcn add`. Оставшиеся — классы ошибок (`AcceptInviteError`, `InstagramApiError`, `MetaAdsApiError`, `OAuthAppCredentialError`) и `PLAN_REGISTRY`: экспортируются намеренно, для `instanceof` в тестах и как документированный контракт.
- **`docker/prisma.config.ts` как unused file** — читается `prisma migrate deploy` из `/prisma-runtime/` в рантайме контейнера, статически невидим. Ровно тот случай, про который предупреждает сам fallow.
- **`fallow targets`: «split high-impact file `src/lib/api-client.ts` (60 LOC), 20 dependents»** — 20 зависимых это признак удачного шва, а не проблемы; дробить 60-строчный модуль незачем.
- **Дублирование внутри `tools/google-ads.ts` (611 tok), `tools/twitter.ts` (581), `tools/google-tag-manager.ts` (510), `tools/youtube.ts` (417)** — тот же недоиспользованный `createFetchTool`, что закрыт задачей #183; регрессии нет, повторно не заводим. `twitter.ts` (25 ручных хендлеров) и `github.ts` (13) остаются кандидатами, если тема вернётся.
- **`simpleParseEmail` cognitive 47** (`src/lib/mcp/email-parser.ts`) — присущая сложность парсера MIME, покрыт тестами, churn низкий.
- **Дублирование в `app/privacy|terms|cookies/page.tsx` и `landing/faq|pricing-faq.tsx`** — совпадает разметка юридических/FAQ-страниц, а не одна и та же ответственность; извлечение общего шаблона сделает правки контента дороже.

### Recurring

Ничего: все находки прошлого прогона (#162–#203) закрыты, ни одна не повторилась.

## Docs

**Scope:** `README.md` манифест · `state/spec/data-model.md` ↔ `prisma/schema.prisma` · `state/spec/screens.md` ↔ `src/app/**/page.tsx` · 4 флоу в `state/spec/flows/` · `state/arch.md` (`## Decisions`) · `state/design/components.md` ↔ `src/components/` · `state/product/product.md` · команды `CLAUDE.md`/`package.json` · `ci-runner.md` ↔ `.github/workflows/`. `state/product/metrics.md` и `pricing.md` не существуют — см. «Not yet written».

### Drift found

| Doc file | Claims | Code reality | Severity | Issue |
|---|---|---|---|---|
| `docs/ci-runner.md:1`, «Migrating an existing project» | CI больше не гоняется в GitHub Actions; первым шагом — удалить `.github/workflows/darkflow-ci-gate.yml` | Воркфлоу на месте, `on: push`/`pull_request` (стр. 25–28), `runs-on: [self-hosted, Linux, X64]` (стр. 43); последние 8 прогонов на `master` (08-25…09-01) — все `success`; `CLAUDE.md` опирается на него, запрещая локальный `pnpm build` | high | #297 |
| `docs/state/spec/flows/billing-checkout.md`, «Опорные точки» | «чекаут Polar периода пока не различает, один product id на план» | `polarProductIdEnvYearly` (`plans.ts:31,55,65`), `getPolarProductId(plan, cycle)` (`:126`), `checkoutSlug()` (`:136`), два слага на план в `auth.ts:44-48`, `getPlanForProductId()` матчит обе env (`:116-120`) — годовая покупка работает с Task #267 (f527900) | medium | #298 |
| `docs/README.md`, «File manifest» | Слой `state/design/` не упомянут ни строкой | `docs/state/design/components.md` — содержательный документ, правился по задаче #234; плюс каталог `state/design/assets/` | medium | #299 |

### Проверено без расхождений

- `data-model.md` — 15 моделей в доке = 15 `model` в `prisma/schema.prisma`, ER-диаграмма полная.
- `screens.md` — 28 строк таблицы = 28 `page.tsx` в `src/app/`, колонка «Облако» соответствует гейтам `notFound()`.
- `arch.md` `## Decisions` — D-0001…D-0014, ни одно решение не противоречит коду; D-0007 корректно помечен как замещённый D-0012.
- `flows/connect-service.md`, `flows/mcp-tool-call.md`, `flows/invite-onboarding.md` — совпадают с кодом.
- `design/components.md` — список примитивов = `ls src/components/ui`, состав каталогов совпадает.
- Команды `dev`/`build`/`lint`/`test` из `CLAUDE.md` присутствуют в `package.json`.

### Минорное (без задач)

- `CLAUDE.md` пишет «26 providers» в трёх местах; в `ServiceProvider` и `PROVIDER_REGISTRY` их 27 (`product.md` считает верно).
- Пустой каталог `docs/state/spec/screens/` рядом с файлом `screens.md` — свёрнут в план задачи #299.
- Легаси-секции `## Изменения` и `## Логи (SigNoz)` в старых логах вместо `## Changes` / `## Performance`; новые логи пишут по таксономии `agent-workflow.md`.

### Not yet written

- `state/product/metrics.md` — писать нечего: инструментирования аналитики в `src/` нет ни строки (`rg 'openpanel|op.track|posthog' src/` пусто), см. H-003.
- `state/product/pricing.md` — цифры живут в `src/lib/plans.ts` (D-0011) и на `/pricing`; отдельный документ не заводился.
- `state/product/positioning.md` — позиционирование лежит внутри `product.md`.

### Recurring Issues

Нет: все находки прошлого аудита (#234–#239, 2026-08-15) закрыты и не регрессировали.

### Hypotheses

- **H-004** (дрейф `state/` копится волнами, а не постепенно) — 2/3, `tracking`. Волна после #234–#239 (17.08→01.09: годовой чекаут f527900, YouTube Analytics 7cb352e, BYO в start-роуте e610725) снова не тронула `docs/state/` и оставила ровно одну новую дрейф-находку (#298). Ледджер: `docs/state/hypotheses.md`.
- **H-003** (нет инструментирования аналитики) — подтверждается повторно: событий в `src/` по-прежнему нет.

### Recommendations

1. `docs/ci-runner.md` → переписать под работающий гейт (воркфлоу остаётся, `ci-watch` читает его conclusion) → критерий: доку нельзя прочитать как инструкцию удалить зелёный CI.
2. `docs/state/spec/flows/billing-checkout.md` → описать пару слагов на план и годовой product id → критерий: в доке нет фразы «один product id на план».
3. `docs/README.md` + `docs/agent-workflow.md` → внести слой `state/design/` → критерий: каждый непустой файл под `docs/state/` есть в манифесте.

## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-08-31 06:00 → 2026-09-01 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 55 613 | 58 089 |
| Ошибок (error rate) | 0 (0%) | 16 (0.03%) |
| p99 самого медленного эндпоинта | `GET /login` 437 ms | `GET /api/mcp/[apiKey]` — артефакт SSE-стрима (держится часами, keep-alive каждые 30с), реальный «медленный» — `mcp.tool threads_publish_thread` 20.9s |
| Медленные DB-запросы (>100ms) | 0 (макс. 10.4ms) | 0 (макс. 259ms на McpEndpoint lookup, разово) |
| Запросов за 7 дней (cloud) | 282 776 (~40.4к/сут в среднем) | — |

### Findings

- **[youtube] `youtube_get_analytics` стабильно 403 на self-hosted** — `YouTube Analytics API has not been used in project 953547536587 or it is disabled`. Не проблема скоупа (yt-analytics.readonly добавлен в 7cb352e, 24.08) — сам API не включён в Google Cloud Console для проекта GOOGLE_CLIENT_ID. → задача #300.
- **[integrations] Google Ads-подключение отозвано** (`googleAds token refresh failed (400)`) — штатное поведение классификатора `classifyOAuthError` (invalid_grant → revoke + уведомление), не баг.
- **[latency] `mcp.tool threads_publish_thread` (20.9s) и `instagram_create_post` (20.5s)** близко к 30s-таймауту тула — ожидаемо: опрос готовности контейнера у Meta (до 25s бюджета), задокументировано в CLAUDE.md. Задачу не заводили — нет простого фикса без переделки на асинхронную модель.
- Боевой (404/401) шум на обоих сервисах — сканирование ботами (`wp-json`, `.php`, `.well-known/*`) и попытки подбора MCP API-ключей; оба пути уже защищены (brute-force rate-limit в `api-keys.ts`, fail-open rate limiter).

### Recurring Issues

Нет — это первый прогон, где встретилась 403-находка по YouTube Analytics API.

### Hypotheses

Не заводились.

### Recommendations

1. `youtube_get_analytics` 403 → включить YouTube Analytics API в Google Cloud Console для проекта 953547536587 → критерий: тул возвращает 200 для существующего подключения (задача #300).
