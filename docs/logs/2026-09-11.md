## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-10 06:00 → 2026-09-11 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 31 778 | 49 315 |
| Ошибок (error rate) | 0 (0.00%) | 25 (0.05%) |
| p99 самого медленного эндпоинта | `POST /` 480.9ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (keep-alive держит соединение часами); реальный «медленный» — `mcp.tool instagram_create_post` 20.8s |
| Медленные DB-запросы (>100мс) | 0 (макс. 9.9ms) | 0 (макс. 176.6ms — `McpEndpoint` lookup) |
| Запросов за 7 дней | 249 439 (~35.6к/сут) | 300 499 (~42.9к/сут) |
| Объём за 24ч vs 7-дн. среднее | −10.8% | +14.9% |

### Findings

- **[DB] `McpEndpoint` lookup p99 176.6ms на `scopegate-selfhost`** — вернулся ниже порога 200ms третий день подряд после H-008 (1/3: 444.8ms, 2/3: 233.0ms). Закрыта как refuted, тот же on/off-паттерн, что и H-007.
- 25 ошибок на `scopegate-selfhost` — штатные паттерны: `googleAds_get_campaign_performance`/`youtube_upload_video` на revoked-коннектах (`invalid_grant`, уведомление уже отправлено), `threads_publish_thread` timeout (>3500ms, retry покрывает), единичные Gmail 429 (`Too many concurrent requests for user`). Не дефект.
- `mcp.tool instagram_create_post` (20.8s) / `threads_publish_thread` (14.3s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.
- `mcp.tool googleAds_get_search_terms_report` (9.8s) / `searchConsole_inspect_url` (6.7s) — тот же паттерн, что закрыт как H-001 (confirmed/external), в рамках намеренного таймаута.
- Боевой (400/404) шум на обоих сервисах — сканирование ботами (`.well-known/*`, `wp-admin/install.php`, `.env`, `.git/config`) и подбор MCP API-ключей на `scopegate-cloud`; уже задокументированный паттерн, оба пути защищены.

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- **H-008 закрыта** (refuted) — `McpEndpoint` lookup latency: 3-й прогон подряд вернулся ниже 200ms (176.6ms), устойчивой регрессии не подтвердилось. См. `docs/state/hypotheses.md`.

### Recommendations

Нет рекомендаций уровня medium+ — оба сервиса в норме, все находки — известные паттерны ниже порога заведения задачи.
