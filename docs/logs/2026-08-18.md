# 2026-08-18

## Performance

**Tool:** SigNoz
**Период:** 2026-08-17 06:00 → 2026-08-18 06:00 UTC (24ч), сервисы `scopegate-cloud` и `scopegate-selfhost`

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 34019 | 76372 |
| Ошибок | 0 (0.00%) | 35 (0.05%) |
| p99 латентность (топ, кроме известных паттернов) | `GET /pricing` 1812.1мс | `GET /api/mcp/[apiKey]` — SSE keep-alive, не регрессия |
| Медленные DB-запросы (>100мс) | 0 — все ≤9.1мс | 0 — все ≤48.2мс |

### Findings

- **`GET /pricing` p99 1812мс** на `scopegate-cloud` — первое наблюдение этого маршрута в наблюдательных прогонах (не встречался в снапшотах 08-11…08-17). Ниже порога для задачи → **H-005** (1/3), отслеживается.
- `mcp.tool searchConsole_inspect_url` p99 30014.9мс — тот же паттерн, что закрыт как H-001 (confirmed/external, GSC API намеренно на таймауте 30с), фикса не требует.
- `mcp.tool threads_publish_thread` (14650мс) и `mcp.tool instagram_create_post` (21442мс) — ожидаемо, укладываются в бюджет polling'а контейнера Meta (25с, `waitForContainerReady()`); тот же паттерн, что и threads в прошлых снапшотах.
- 204-null-body краш (задача #254, закрыта 08-16) — в `error_log_samples` обоих сервисов за это окно не встречается, фикс подтверждён в проде.
- 400/401 шум (`/[blocked-action]` — боты с невалидным `next-action`, `/api/mcp/[apiKey]` 401 — неавторизованные пробы ключа) — уже намеренно агрегируется/рейт-лимитится кодом, не дефект.
- Ошибки в логах `scopegate-selfhost` — все внешние (YouTube 403, Google Ads revoked-connection) — ожидаемое поведение reconnect-флоу, не дефект платформы.

### Recurring Issues

—

### Hypotheses

- H-001 — closed (confirmed/external), см. `docs/state/hypotheses.md`
- H-005 — GET /pricing latency, 1/3, новая

### Recommendations

—
