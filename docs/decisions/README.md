# Decisions (ADRs)

Accepted architectural decisions. One file per decision, based on `TEMPLATE.md`
(context → decision → how to verify). Read this index before any architectural
change to avoid re-deciding something already settled or contradicting it.

Naming: `NNNN-short-title.md` (e.g. `0001-use-postgres.md`), zero-padded, incrementing.

## Index

| # | Decision | Status | Date |
|---|---|---|---|
| — | *No ADRs yet. Copy `TEMPLATE.md` to `0001-...md` and add the first row here.* | — | — |

Status values: `accepted` · `superseded by #N` · `deprecated`.
