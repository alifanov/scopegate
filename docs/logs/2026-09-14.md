## Changes

- Task #322 — `/api/csp-report`: 64 KB body cap, 10-report cap per request, 30 req/60s per-IP rate limit (db48433)

## Performance

**Target:** https://scopegate.dev  ·  **Strategy:** mobile  ·  **Source:** lab (local Lighthouse)

### Metrics
| Time | LCP | TBT | CLS | TTFB | FCP | Score | Result | Task |
|---|---|---|---|---|---|---|---|---|
| 10:47 | 2.39s | 26ms | 0.00 | 70ms | 1.30s | 98 | ok | — |

### Notes
Все метрики в зоне good. Замер выполнен через локальный `pnpm add -D lighthouse` в скретч-директории — глобальный `pnpm dlx` в сессии был сломан (x64/Rosetta node в PATH ломает pnpm dlx cache); обошли через локальную установку зависимости.

## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-13 08:00 → 2026-09-14 08:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 46 895 | 38 981 |
| Ошибок (error rate) | 0 (0.00%) | 2 (0.01%) |
| p99 самого медленного эндпоинта | `GET /_next/static/chunks/...js` 56.8ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (keep-alive держит соединение часами); реальный «медленный» — `mcp.tool instagram_create_post` 20.8s |
| Медленные DB-запросы (>100мс) | 0 (макс. 12.5ms) | 0 (макс. 42.0ms — `McpEndpoint` lookup) |
| Запросов за 7 дней | 260 702 (~37.2к/сут) | 283 911 (~40.6к/сут) |
| Объём за 24ч vs 7-дн. среднее | +25.9% | −3.9% |

### Findings

- Объём на `scopegate-cloud` +25.9% к 7-дневному среднему — крупнее обычных суточных качелей, но без роста ошибок или деградации латентности (p99 самой медленной страницы всё ещё 56.8ms); ниже порога 3 прогонов/2 источников, задачу не заводил.
- `McpEndpoint` lookup на `scopegate-selfhost` — 42.0ms, стабильно ниже 200ms-порога (H-008/H-007 закрыты как refuted, паттерн не повторился). Без изменений.
- 2 ошибки на `scopegate-selfhost` — `error_log_samples` пуст, в пределах штатного шума.
- `mcp.tool instagram_create_post` (20.8s) / `threads_publish_thread` (14.6s) / `youtube_upload_video` (5.5s) — в рамках 25s-бюджета опроса контейнера Meta и внешних медиа-аплоадов, задокументировано в CLAUDE.md; не новая находка.
- Боевой (400/404) шум на обоих сервисах — сканирование ботами (`.env`, `.azure/credentials`, `/fetch`, `/proxy`) и подбор MCP API-ключей (602+86× 401 на `scopegate-cloud` `/api/mcp/[apiKey]`); уже задокументированный паттерн, оба пути защищены.

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

Нет активных гипотез, ждущих новых данных (H-003/H-004 без изменений, см. `docs/state/hypotheses.md`).

### Recommendations

Нет — метрики в норме, критичных/high/medium находок не обнаружено.
