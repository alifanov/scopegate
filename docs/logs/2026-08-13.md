# 2026-08-13

## Analytics

OpenPanel MCP не подключён к проекту (`.mcp.json` содержит только `google-ads`), и в коде нет ни одного трекинг-сниппета (`grep -ri openpanel/analytics/track src/` — пусто). Данных по воронке нет физически, не только в этой сессии. Funnel-таблицы, сегментация и снапшот в `analytics.json` в этом прогоне не заполнены (все поля `null`) — по правилу Step 1 «нет MCP → смотрим только коммиты».

### Коммиты за 7 дней, релевантные воронке

- `d4e3616` — лендинг теперь ведёт в регистрацию, копия приведена к продукту
- `54e575b` — маркетинг-сайт перенесён из scopegate-cloud, добавлен cloud-режим
- `f015f8e` — вход через Google + magic link для cloud-режима
- `bfca673` — платные планы на Polar.sh с проверкой лимитов
- `0afc371`, `649879f`, `4f1f78e` — BYO OAuth credentials (ввод собственных кредов)

Все пять — прямые изменения top-of-funnel (лендинг → регистрация → вход → биллинг). Ни одно не измеримо: нет данных о том, растёт ли регистрация после d4e3616, или ниже ли она, чем до перевода лендинга.

`docs/state/product/product.md` всё ещё описывает продукт как «закрытую систему без лендинга и биллинга» — устарел относительно cloud-режима; это для `/darkflow:docs-audit`, не трогаю здесь.

### Ledger

Первое наблюдение (1/3) — добавлено в `docs/state/hypotheses.md` как H-003. Задача не заведена: ниже порога и не имеет числового finding для приоритезации.
