# 2026-08-11

## Security

**Period:** 2026-07-27 → 2026-08-11 (172 файла, +4087/−1568 с прошлого аудита)

### Findings

| Category | Finding | Severity | File / Config | Source |
|---|---|---|---|---|
| Dependency | `nanoid@3.3.16` — бесконечный цикл в `customAlphabet`/`customRandom` при `size=0` (GHSA-2v37-7h3g-55p8) | high | pnpm-lock.yaml | github |
| Dependency | `ip-address@10.2.0` — 3 GHSA о неверной классификации адресов (leading-zero, CIDR-суффикс, NAT64) | high | pnpm-lock.yaml | github |
| Dependency | `brace-expansion@5.0.8` — DoS в обход митигации CVE-2026-14257 (GHSA-rgw5-rvv9-x895) | high | pnpm-lock.yaml | github |
| Dependency | `fast-uri@4.1.1` — host confusion через обратный слэш в authority (GHSA-7p8r-x3mc-p8w7) | high | pnpm-lock.yaml | github |
| Dependency | `hono@4.12.32` — утечка SSR-вывода между запросами через `memo()` + 2 DoS + Proxy Helper | medium | package.json | github |
| Auth | Активация инвайта хеширует пароль напрямую, минуя `minPasswordLength` better-auth — пароль из 1 символа проходит | medium | `src/lib/accept-invite.ts:78` | code-review |
| Dependency | `body-parser@2.2.2` — невалидный `limit` молча отключает проверку размера (GHSA-v422-hmwv-36x6) | low | pnpm-lock.yaml | github |

Code scanning и secret scanning на репозитории выключены — находок с этих поверхностей нет.

### Recurring Issues

- **`fast-uri` — третий аудит подряд** (закрытые #40 → #192 → сегодняшняя #213). Каждый раз host confusion, каждый раз новый обход парсера authority. Пакет транзитивный (через ajv-цепочку), своей защиты у нас на нём не построено, но частота говорит сама за себя.
- **`ip-address` — второй раз** (#44 → #211). Оба раза про классификацию адресов, то есть про поверхность SSRF. Наш `safe-fetch.ts` проверяет диапазоны собственными регулярками и от библиотеки не зависит — это и есть причина, по которой рецидив не превращается в инцидент.

### Recommendations

1. **Слабый пароль на единственном пути регистрации** → задать `minPasswordLength: 12` в `src/lib/auth.ts` и проверять длину в `accept-invite.ts` до хеширования → `POST /api/auth/accept-invite` с коротким паролем отдаёт 400 и не создаёт пользователя (задача #215).
2. **Пять уязвимых зависимостей** → поднять `nanoid ≥3.3.17`, `ip-address ≥10.3.1`, `brace-expansion ≥5.0.9`, `fast-uri ≥4.1.2`, `hono ≥4.12.34`; у `hono` в package.json стоит `~4.12.32`, тильда не пустит на .34 — диапазон нужно расширить → `pnpm why` не показывает уязвимых версий, Dependabot alerts закрыты (задачи #210–#214).

### Что проверено и оказалось закрыто

Findings прошлых аудитов, подтверждённые как исправленные в коде этого периода — не задачи, а фиксация того, что поверхность действительно сузилась:

- SSRF в IMAP/SMTP: новый `src/lib/mcp/mail-safety.ts` пинует IP через `resolveSafeIp` и разрешает только порты 143/993 и 25/465/587; все четыре места создания транспорта (`email.ts:83,325,419,420`) идут через него.
- Path traversal в MCP-инструментах: `service-fetch.ts` добавил `assertSafePath` (декодирует и ловит `..`) и `assertWithinBase` (финальный URL обязан остаться под origin+base-path провайдера).
- Кросс-провайдерные права: `getToolsByActions(actions, provider)` и `validateEndpointPermissions(permissions, providerKey)` теперь сверяют действие с провайдером подключения — инвариант больше не живёт только в React-диалоге.
- GAQL-инъекция: `src/lib/mcp/gaql.ts` — билдер, где каждое значение проходит zod-схему; в `tools/google-ads.ts` не осталось ни одной интерполяции параметра в текст запроса.
- Небезопасные дефолты секретов: `docker-compose.yaml` перешёл на `${BETTER_AUTH_SECRET:?}` / `${ADMIN_PASSWORD:?}`, а `assertAuthSecretIsStrong()` роняет приложение на старте при коротком или публично известном секрете.

## Build

**Scope:** `Dockerfile`, `Dockerfile.cron`, `.dockerignore`, `docker-entrypoint.sh`, `docker-compose.yaml`, `docker/crontab`, `docker/prisma.config.ts`, `package.json` (pnpm 10.9.0), `next.config.ts`, `.github/workflows/darkflow-ci-gate.yml`, `Makefile`.

### Pipeline overview

Push в `master` → CI-gate на self-hosted runner'е (`install` → `prisma generate && tsc --noEmit` → `lint` → `test`, ~60–70 с, последние 15 прогонов: 13 success / 2 failure) и параллельно Coolify пересобирает трёхстейджевый `Dockerfile` на своей VPS (`deps` → `builder` → `runner`, node:22-slim, standalone-вывод 39 МБ + 1.5 МБ static). Реестра нет — образ собирается и запускается на одном хосте, поэтому push/pull слоёв в стоимость деплоя не входит. Миграции идут не на сборке, а на старте контейнера (`docker-entrypoint.sh` → `prisma migrate deploy` из `/prisma-runtime`). Тяжёлые шаги уже прикрыты BuildKit-маунтами: pnpm store в `deps`, `.next/cache` в `builder`.

### Opportunities found

| Area | Current | Proposed | Est. savings | Severity | Issue |
|---|---|---|---|---|---|
| Docker layer cache | Слой установки Prisma CLI закэширован по всему `package.json` (49 изменений за 90 дней), без npm cache mount | Вынести версию Prisma в отдельный файл + `COPY --from=deps`, добавить `--mount=type=cache,target=/root/.npm` | ~198 МБ / 21 с скачивания на каждый второй пуш | medium | #216 |
| Docker layer cache | `COPY prisma ./prisma/` в `deps` перед `pnpm install` — миграция роняет слой установки | Убрать копию (в Prisma 7 нет `postinstall`) | ~20–40 с на коммитах с миграцией | medium | #216 |

### Recurring

Нет. Прошлые находки (#92 pnpm store cache, #120 CI-гейт, #145 `.next/cache` mount, #146 build-шаг в CI, #197 живой runner) закрыты и не откатились — все четыре механизма на месте в текущих файлах.

### Recommendations

1. **`Dockerfile:40–44` + `Dockerfile:9`** → отвязать установку Prisma CLI от содержимого `package.json` (кэш по версии, а не по файлу) и добавить npm cache mount; убрать лишний `COPY prisma ./prisma/` из `deps` → коммит с правкой `pnpm.overrides` показывает `CACHED` на шаге установки Prisma CLI, коммит с новой миграцией — `CACHED` на `pnpm install` (задача #216).

### Проверено и отклонено

Гипотезы, которые замер не подтвердил — фиксируются, чтобы следующий аудит их не переоткрывал:

- **Вырезать `@prisma/studio-core` из runtime-образа (26 МБ + `effect` 32 МБ + `@electric-sql` 23 МБ).** CLI Prisma 7 требует его эагерно: `rm -rf node_modules/@prisma/studio-core && prisma migrate deploy --help` → `Cannot find module '@prisma/studio-core/data/bff'`. То же с `@prisma/dev`. 198 МБ в образе неустранимы без отдельного migration-контейнера, который на одном хосте только добавит диска.
- **Пропускать сборку на docs-only пушах** (11 из 60 последних коммитов). Со стороны Docker уже бесплатно: `.dockerignore` исключает `docs`, `*.md`, `.darkflow.d`, поэтому контекст сборки не меняется и все слои попадают в кэш. Остаётся только ~70 с прогона CI — ниже порога задачи.
- **`start_period: 60s` в healthcheck затягивает деплой.** Нет: Docker в start-периоде опрашивает с `start_interval` (5 с по умолчанию), контейнер помечается healthy заранее.
- **`concurrency`-группа в CI-гейте** и двойной `prisma generate` (шаг `build` + `pretest`) — экономия секунды-две на прогон, ниже порога задачи.

## Performance

**Target:** https://scopegate.dev  ·  **Strategy:** mobile  ·  **Source:** lab (local Lighthouse, native arm64 node)

### Metrics
| Time | LCP | TBT | CLS | TTFB | FCP | Score | Result | Task |
|---|---|---|---|---|---|---|---|---|
| 22:14 | 27.5s | 111.4s | 0.18 | 38ms | 1.8s | 26 | inconclusive | — |
| 22:16 | 29.3s | 104.6s | 0.16 | 39ms | 1.8s | 28 | inconclusive | — |

### Notes

LCP/TBT/CLS отброшены как недостоверные — известная проблема этой машины (см. `docs/insights/web-vitals/2026-07-28.md`, задача #198): `runWarnings` оба прогона сообщают "slower CPU than expected", `vm.loadavg` во время замера — 14–15 (десятки фоновых dev-агентов). Ранее это уже давало ложный LCP 24.6s/score 28 на этом же URL при полном отсутствии дефекта в коде — `/` редиректит на тривиальную форму `/login` без тяжёлых скриптов. TTFB (не зависит от CPU) стабилен и низок на обоих прогонах — 38–39мс, сервер отвечает быстро. Задача не заведена: без чистой (без контеншна) машины показатели непригодны для триажа.
