# Observability Review — 2026-06-29

**Tool:** SigNoz (self-hosted)
**Period:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs вчера (2026-06-28) |
|---|---|---|
| Error rate | 0 ошибок (0.0%) | ✅ без изменений |
| p99 латентность (топ, без SSE) | 292 мс (openRouter_get_credits) | ~стабильно |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ без изменений |
| Объём запросов (спанов) | 18 807 | +22% vs вчера (15 370) |

## Findings

### Ошибки

Ошибок не обнаружено ✅. HTTP-ошибок нет. Error logs пусты.

### Латентность

- **`GET /api/mcp/[apiKey]`** — p99 4 571 901 мс (~76 мин): ожидаемый артефакт SSE keep-alive соединений; не является проблемой.
- **`GET` (orphaned span)** — p99 960 502 мс (~16 мин): снизилось с ~30 мин вчера до ~16 мин сегодня; OTel-артефакт без атрибута `http.route`. Тренд нисходящий — норма.
- **`mcp.tool openRouter_get_credits`** — p99 292 мс: в норме.
- **`POST /api/cron/refresh-tokens`** — p99 279 мс: батч-операция, приемлемо.
- **`refresh_connection`** — p99 262 мс: стабильно, чуть ниже вчерашних 275 мс.
- **`oauth.fetch_token`** — p99 262 мс: норма.
- **`service-fetch openRouter`** — p99 244 мс: норма.

### База данных

Все запросы значительно ниже порогов:
- `ServiceConnection UPDATE` — p99 20.8 мс ✅
- `McpEndpoint SELECT` — p99 16.4 мс ✅
- `rate_limit_bucket INSERT ON CONFLICT` — p99 13.5 мс ✅
- `ServiceConnection SELECT` — p99 11.5 мс ✅
- `EndpointPermission SELECT` — p99 7.7 мс ✅

### Объём запросов

18 807 спанов за 24 ч vs 15 370 вчера (+22%). Рост ожидаем: понедельник после воскресенья — рабочий день. 7-дневный тренд стабилен.

### Интеграции

Трейсы присутствуют: MCP SSE, cron-рефреш (openRouter), Prisma DB. Все ожидаемые сервисы репортируют.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| GET (orphaned span) | 5+ | p99 снизилось с 30 до 16 мин; артефакт OTel; тренд улучшается |
| 0 ошибок | 8+ дней подряд | ✅ Стабильно |

## Hypotheses

- **openRouter латентность** — `service-fetch openRouter` p99 244 мс и `oauth.fetch_token` 262 мс — стабильная базовая линия третью неделю. Если превысит 500 мс → создать issue.
- **Orphaned `GET` span** — p99 снизилось с 1 814 497 мс до 960 502 мс (−47%). Гипотеза: артефакт накапливает меньше долгих SSE-соединений в первой половине понедельника. Наблюдение продолжается.

## Recommendations

1. **Без действий** — все производственные метрики в норме, ошибок нет, DB здорова.
2. **Мониторинг orphaned span** — тренд снижается; если p99 > 2 М мс при нормальном объёме — аудит OTel middleware.
3. **openRouter latency** — зафиксировать базовую линию ~250 мс как ожидаемую; алерт при > 500 мс.

## Новые GitHub Issues

Не созданы — все метрики в норме, новых проблем не выявлено.
