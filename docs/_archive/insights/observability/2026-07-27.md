# Observability Review — 2026-07-27

**Tool:** SigNoz (self-hosted, `signoz.chatindex.app`)
**Период:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs 2026-07-19 (прошлый снапшот) |
|---|---|---|
| Error rate | 0 ошибок (0.0%) | 🟢 было 61 (0.14%) — полностью чисто |
| p99 латентность (топ, без SSE) | 530.2 мс (`mcp.tool gmail_search_emails`) | 🟢 было 756.6 мс, и в целом топ намного спокойнее — ни threads, ни linkedin publish не попали в топ-10 за эти сутки |
| Медленные DB-запросы (> 50 мс) | 0 | 🟢 было 107.0 мс (Project aggregate) — теперь максимум 45.6 мс |
| Объём запросов (спанов) | 12 381 | 🟢 в норме: 7-дневное среднее = 12 271/сутки (85 896/7), отклонение +0.9%. Прошлый снапшот (44 328) был аномальным пиком, вызванным запуском Instagram/LinkedIn-документов — с тех пор объём вернулся к базовому уровню |

## Findings

Значимых находок нет — чистые сутки: 0 ошибок, 0 HTTP 4xx/5xx, все DB-запросы быстрее 50 мс, все инструментальные (`mcp.tool.*`) latency в пределах нормы.

### Закрытые с прошлого раза

- **Задача #186** (try/catch + логирование в `POST /api/cron/refresh-tokens`) — закрыта. Маршрут за сегодня отработал 0 ошибок.
- Инцидент с Prisma-enum `instagram` (закрыт `502db0d`) — рецидивов не наблюдалось за 3 снапшота подряд.

### Ошибки

Не обнаружено. `http_errors` за 24ч — пустой список.

### Латентность

| Эндпоинт | p99 | Оценка |
|---|---|---|
| `GET /api/mcp/[apiKey]` | ~4.0 мин | ✅ SSE keep-alive — ожидаемое поведение долгоживущего соединения, не реальная latency запроса |
| `mcp.tool gmail_search_emails` | 530.2 мс | 🟢 |
| `POST /api/cron/refresh-tokens` | 290.7 мс | 🟢 |
| `cron.refresh_tokens` | 281.6 мс | 🟢 |
| `refresh_connection` / `oauth.fetch_token` | 263.1 мс | 🟢 |
| `service-fetch gmail` | 205.5 мс | 🟢 |
| `POST /api/mcp/[apiKey]` | 163.6 мс | 🟢 |

Threads/LinkedIn/Instagram publish-инструменты не вызывались в это окно — данных нет, не аномалия (просто не было активности).

### База данных

| Запрос | p99 | Оценка |
|---|---|---|
| `SELECT ServiceConnection` | 45.6 мс | ✅ |
| `SELECT pg_advisory_xact_lock(...)` | 33.5 мс | ✅ (ожидаемая сериализация `withConnectionLock`) |
| `UPDATE ServiceConnection` | 22.6 мс | ✅ |
| `SELECT McpEndpoint` | 14.1 мс | ✅ |
| `INSERT rate_limit_bucket ... ON CONFLICT` | 14.0 мс | ✅ |

### Объём запросов

12 381 спанов за 24ч — совпадает с 7-дневным средним (12 271/сутки, +0.9%). Пик 44 328 из прошлого снапшота (19.07) был временным (запуск новых интеграций), сейчас трафик стабилизировался.

### Интеграции

За 7 дней репортят спаны: Gmail, Threads, Instagram, LinkedIn, Google Ads, Search Console. Пробелов в трейсинге не обнаружено.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| `threads_publish_thread` p99 (24 039 → 11 102 → 11 548 мс) | 3 снапшота стабильно | ✅ Issue #173 закрыт, сегодня инструмент не вызывался — нет новых данных, но регрессии нет |
| `POST /api/cron/refresh-tokens` 500-ошибки | 0 инцидентов с 19.07 | ✅ Задача #186 закрыта, фикс подтверждён |

## Hypotheses

Нет новых гипотез — прошлые (`linkedin_create_post` latency, `SELECT Project` aggregate) не воспроизвелись сегодня из-за отсутствия вызовов/попаданий в топ; данных недостаточно, чтобы закрыть или подтвердить, продолжаем наблюдение.

## Recommendations

Новых задач не создано — метрики за сутки в норме по всем категориям (ошибки, latency, DB, объём, интеграции). Рекомендация: продолжать регулярные снапшоты (последний перед этим был 8 дней назад, 19.07 — стоит проверить, почему routine `observability-check` пропускал запуски, чтобы не терять непрерывность рядов).
