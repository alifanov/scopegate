## Performance

**Tool:** SigNoz
**Период:** 2026-08-18 06:00 → 2026-08-19 06:00 UTC (24ч), сервисы `scopegate-cloud` и `scopegate-selfhost`

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 32911 (вчера 34019) | 47913 (вчера 76372) |
| Ошибок | 0 (0.00%) | 0 (0.00%) |
| p99 латентность (топ, кроме известных паттернов) | `GET /scopegate-demo.mp4` 834.9мс | `GET /api/mcp/[apiKey]` — SSE keep-alive, не регрессия |
| Медленные DB-запросы (>100мс) | 0 — все ≤9.6мс | 0 — все ≤37.2мс |

### Findings

Чисто — ошибок нет на обоих сервисах, все DB-запросы в норме, ни один эндпоинт не превышает порог 1с.

- `GET /pricing` из вчерашнего снапшота (H-005, 1812мс) сегодня отсутствует даже в топ-10 медленных на `scopegate-cloud` (10-е место — 30.9мс) — не воспроизвелось, гипотеза закрыта как refuted (одноразовый всплеск).
- `mcp.tool instagram_create_post` (20215мс) и `threads_publish_thread` (15286мс) на `scopegate-selfhost` — ожидаемо, укладываются в бюджет polling'а контейнера Meta (25с, `waitForContainerReady()`); тот же паттерн, что и в прошлых снапшотах.
- `GET /api/mcp/[apiKey]` p99 5652246.2мс на `scopegate-selfhost` — артефакт долгоживущего SSE-соединения (keep-alive пинги каждые 30с), не регрессия; тот же паттерн, что фиксировался ранее.
- 400/401/404 шум (`/wp-admin/install.php`, `/.env`, `/proxy`, `/api/mcp/[apiKey]` 401) — бот-сканирование и неавторизованные пробы ключа, уже рейт-лимитится, не дефект.
- Запросов на `scopegate-selfhost` заметно меньше, чем вчера (47913 против 76372, −37%) — весь трафик в целевых окнах состоит преимущественно из бот-шума (404 на известные пути сканеров), колебание не тянет на инцидент; ниже порога для отдельной гипотезы.

### Recurring Issues

—

### Hypotheses

- H-005 — закрыта как refuted, см. `docs/state/hypotheses.md`

### Recommendations

—
