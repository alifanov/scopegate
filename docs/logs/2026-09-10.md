## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-09 06:00 → 2026-09-10 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 32 930 | 38 570 |
| Ошибок (error rate) | 0 (0.00%) | 3 (0.01%) |
| p99 самого медленного эндпоинта | `GET /signup` 591.5ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (keep-alive держит соединение часами); реальный «медленный» — `mcp.tool threads_publish_thread` 21.1s |
| Медленные DB-запросы (>100мс) | 0 (макс. 11.5ms) | 1 — `McpEndpoint` lookup 233.0ms |
| Запросов за 7 дней | 248 525 (~35.5к/сут) | 295 008 (~42.1к/сут) |
| Объём за 24ч vs 7-дн. среднее | −7.2% | −8.4% |

### Findings

- **[DB] `McpEndpoint` lookup p99 233.0ms на `scopegate-selfhost`** — второй день подряд выше порога 200ms (H-008: 1/3 09-09 444.8ms → 2/3 09-10 233.0ms). Ещё не дошло до порога 3 прогонов, задачу не заводил.
- `GET /signup` 591.5ms на `scopegate-cloud` — в том же диапазоне, что 08-24 (633.3ms) и 08-26 (585.7ms); повторяющийся, но не растущий паттерн этой страницы, ранее уже не признан регрессией, новой находки нет.
- 3 ошибки на `scopegate-selfhost` — штатный `threads_publish_thread` timeout (>3500ms), retry-логика уже покрывает. Не дефект.
- `mcp.tool threads_publish_thread` (21.1s) / `instagram_create_post` (20.9s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.
- Боевой (400/401/404) шум на обоих сервисах — сканирование ботами (`.well-known/*`, `.env`, `.git/config`) и подбор MCP API-ключей; уже задокументированный паттерн, оба пути защищены.

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- H-008 — `McpEndpoint` lookup DB-latency вспышка на `scopegate-selfhost`: 2/3 (233.0ms, всё ещё выше 200ms). Ледджер: `docs/state/hypotheses.md`.

### Recommendations

Нет рекомендаций уровня medium+ — оба сервиса в норме.
