# Observability Review — 2026-06-17

**Tool:** SigNoz (self-hosted — https://signoz.chatindex.app)
**Period:** last 24 h
**Service:** scopegate

## Key Metrics

| Metric | Value | vs yesterday |
|---|---|---|
| Total requests | 33 620 | — (window: 24 h vs 7 d) |
| Error rate | 0.36% (120 errors) | Stable (~0.4% prev) |
| p99 latency (threads_publish_thread) | 18 755 ms | ↑ +88% vs yesterday 9 977 ms |
| Slow DB queries (> 100 ms) | 0 | ↓ Resolved (#124 index applied) |
| Open SigNoz issues | 5 | +2 new (#132, #133) |

## Findings

### Errors

- **16 × /api/mcp/[apiKey] → 500** — unhandled server errors on MCP endpoint. Error log samples empty — exact cause unknown without trace inspection. Issue #132 (MEDIUM) created.
- **3 × / → 404** — root path probes (bots/scanners). Persistent, low severity.
- **4xx/5xx via Meta Graph API** — issue #109 (HIGH) still open; Meta errors likely still occurring but not captured in 24 h window shown.

### Latency

- **GET /api/mcp/[apiKey]** p99 = 17 470 758 ms (~4.85 h) — intentional SSE keep-alive stream. Not an actual slow request. Ignore.
- **GET (unnamed span)** p99 = 1 815 101 ms (~30 min) — same pattern as previous snapshots; long-lived connection artifact. Monitoring.
- **mcp.tool threads_publish_thread** p99 = **18 755 ms** — worsened significantly vs yesterday (9 977 ms). Now at 62% of the 30 s tool timeout. Issue #133 (MEDIUM) created.
- **service-fetch threads** p99 = 10 731 ms — Threads API latency is the primary contributor to the tool's slowness.
- **POST /api/mcp/[apiKey]** p99 = 5 111 ms — acceptable for MCP JSON-RPC calls.
- **mcp.tool linkedin_create_post** p99 = 1 825 ms — stable, acceptable.
- **GET /api/oauth/threads/callback** p99 = 1 506 ms — OAuth exchange slow. Issue #111 covers Google; Threads may be similar pattern.

### Database

- All queries under 40 ms. Previous slow `McpEndpoint SELECT` (163 ms) is now 29.5 ms — confirms auto-approved index #124 was successfully applied.
- AuditLog SELECT p99 = 38.4 ms — healthy.
- No slow queries (> 100 ms) detected.

### Integration health

- All services reporting traces. No gaps.
- Error log samples: 0 (log export not connected to SigNoz — traces only).

## Recurring Issues

| Issue | Snapshots in a row | Status |
|---|---|---|
| Meta Graph API 400 errors (#109) | 6+ | HIGH, open |
| Threads API tokens in spans (#108) | 6+ | CRITICAL, open |
| threads_publish_thread p99 > 9 s (#122, #133) | 4 | MEDIUM, now worsening |
| Unnamed `GET` span with multi-hour p99 | 4 | Monitoring, no issue |
| Root `/` 404 probes | 4 | LOW, skipped |

## Hypotheses

- **Unnamed `GET` span p99 ~30 min** — consistently appearing across snapshots, scaling in duration. Likely aggregated SSE or long-polling connections from proxies. Not creating an issue — need to confirm via SigNoz trace inspector that it correlates with `mcp.tool` spans.
- **Threads OAuth callback p99 1.5 s** — not currently in an open issue. If it appears in 2 more consecutive snapshots above 1.5 s, will file a dedicated issue (similar to #111 for Google OAuth).
- **500 errors on /api/mcp/[apiKey]** — 16 occurrences but no log samples. Could be tool-level unhandled exceptions or request body parse errors. Issue #132 filed for investigation.

## Recommendations

1. **#108 (CRITICAL)** — Sanitize Threads API URLs in `service-fetch.ts`; strip `?access_token=` before using as span names. Still unblocked.
2. **#109 (HIGH)** — Meta 400 errors persisting 6+ snapshots. Prioritize auto-revoke + user notification flow for persistent `invalid_grant`-equivalent codes.
3. **#133 (MEDIUM)** — `threads_publish_thread` p99 worsened to 18.7 s. Enforce 8 s timeout on `service-fetch threads` to fail fast instead of hitting 30 s tool limit.
4. **#132 (MEDIUM)** — Investigate 500 errors on `/api/mcp/[apiKey]`: open SigNoz Traces, filter `http.status_code=500`, inspect `exception.message` attributes.
5. **DB resolved** — Index from #124 successfully reduced McpEndpoint SELECT from 163 ms to 29.5 ms. No further DB action needed.
