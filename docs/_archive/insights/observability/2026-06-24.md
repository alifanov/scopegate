# Observability Review — 2026-06-24

**Tool:** SigNoz (self-hosted, signoz.chatindex.app)
**Period:** последние 7 дней (168 ч) | Сервис: `scopegate`

## Key Metrics

| Metric | Value | vs 2026-06-20 |
|---|---|---|
| Всего запросов | 149 612 | −29 140 (−16%) |
| Ошибок (has_error=true) | 25 | −231 (↓90%) ✅ |
| Error rate | 0.02% | ↓ с 0.14% ✅ |
| p99 SSE keep-alive | 16 521 199 мс¹ | = (ожидаемо) |
| p99 threads_publish_thread | 18 650 мс | = (fix 773b2c2 применён, данные включают pre-fix период) |
| p99 linkedin_create_post | 2 008 мс | = |
| HTTP 500 на /api/mcp/[apiKey] | 34 | = (root cause не устранён) |
| HTTP 500 на /api/auth/[...all] | 4 | = (fix применён) |
| Медленных DB-запросов (>100 мс) | 0 | ✅ |

¹ SSE стриминг держит соединение открытым часами — ожидаемое поведение.

## Findings

### Ошибки

- **34 HTTP 500 на `/api/mcp/[apiKey]`** — сохраняются 3+ снапшота подряд. Issue #140 закрыт после улучшения логирования (24498c4), root cause не устранён. Создан issue **#154** (priority:high).
- **4 HTTP 500 на `/api/auth/[...all]`** — issue #142 закрыт. Данные включают pre-fix период; мониторинг в следующем снапшоте.
- **3 HTTP 401 на `/api/auth/[...all]`** — норма для истёкших сессий.
- **27 HTTP 404 на `/`** — боты/сканеры; норма.
- **4 HTTP 400 на `/[blocked-action]`** — middleware работает корректно.

### Позитивное

- **Error rate упал с 0.14% → 0.02%** (−90% span errors) — вероятно результат правки OAuthTokenError: ошибки `invalid_grant` больше не создают ERROR-спаны в SigNoz.
- Все previous issues (#122, #140, #141, #142, #143) закрыты.

### Латентность

- **`mcp.tool threads_publish_thread` p99 = 18 650 мс** — fix 773b2c2 применён (убраны retry, бюджет сокращён). Текущие данные включают pre-fix период. Результат оценить в снапшоте 2026-07-01.
- **`service-fetch threads` p99 = 10 626 мс** — Threads API медленный; ожидаемо при асинхронной обработке контейнеров.
- **`mcp.tool linkedin_create_post` p99 = 2 008 мс** — #141 закрыт; мониторинг.
- **`GET /api/oauth/threads/callback` p99 = 1 506 мс** — #143 закрыт; мониторинг.
- **`mcp.tool twitter_post_tweet` p99 = 926 мс** — приемлемо (<1 с).

### База данных

Все запросы ниже порога 100 мс:
- `SELECT Project` с агрегацией McpEndpoints — p99 = 90 мс (стабильно; индекс `@@index([projectId])` уже есть на McpEndpoint, COUNT-агрегация; деградации нет)
- `INSERT AuditLog` — p99 = 53 мс
- `SELECT McpEndpoint` — p99 = 50 мс
- `SELECT AuditLog` — p99 = 42 мс
- `INSERT rate_limit_bucket` (upsert) — p99 = 37 мс

### Интеграции

Трейсы присутствуют по всем основным интеграциям: Threads, LinkedIn, Twitter, Google OAuth, Prisma DB. Пробелов нет. Error log samples: пусто.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| HTTP 500 на /api/mcp/[apiKey] — 34 шт. | 4+ | #154 создан (priority:high) |
| threads_publish_thread p99 >11 с | 4+ | #122 закрыт, fix применён (773b2c2), мониторинг |

## Hypotheses

- **`/api/auth/[...all]` 500 × 4** — #142 закрыт. Если продолжится в следующем снапшоте — переоткрыть.
- **Снижение трафика −16%** — 149k vs 178k запросов в прошлую неделю. Возможна сезонность или изменение паттернов использования. Отслеживать в следующем снапшоте.
- **Threads OAuth callback p99 = 1 506 мс** — повторяется, но #143 закрыт. Если p99 >2 с — создать новый issue.

## Recommendations

1. **#154 (MCP 500)** — просмотреть структурированные логи с `error_type`/`stack` в SigNoz Logs для `/api/mcp/[apiKey]` (добавлены commit 24498c4), сгруппировать по типу ошибки, устранить top-1 причину.
2. **threads_publish_thread** — подождать снапшота от 2026-07-01 для оценки эффекта fix 773b2c2 (убраны retry, бюджет 8s).
3. **CI issue #153** — TypeScript error `Property 'id_token' does not exist on type 'OAuthTokenResult'` в `oauth-callback-config.ts:39`. Статус `approved`, `fix-ci-issue` worker подхватит.
