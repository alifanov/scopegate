## Performance

**Target:** https://scopegate.dev  ·  **Strategy:** mobile  ·  **Source:** lab (local Lighthouse)

### Metrics
| Time | LCP | TBT | CLS | TTFB | FCP | Score | Result | Task |
|---|---|---|---|---|---|---|---|---|
| 12:56 | 2.39s | 18ms | 0.00 | 44ms | 1.14s | 98 | ok | — |

### Notes
Все метрики в зоне "good" с большим запасом. Прогон через нативный arm64 node (`~/.nvm/versions/node/v22.15.1`) — обходит известный Rosetta-баг с завышением CPU-метрик на этой машине. Load average в норме (~4), `runWarnings` пусты.

## SEO

GSC data skipped: Google Search Console MCP is not connected.
Connect the GSC MCP in your project's .claude/settings.json to enable it.

Кодовый + live-аудит scopegate.dev: robots.txt/sitemap/canonical/title/description/H1/JSON-LD/alt/редиректы/404 — чисто (предыдущие прогоны уже закрыли основное). Одна реальная находка: `openGraph` в 11 `page.tsx` (`/`, `/pricing`, `/features`, `/compare`, `/compare/[slug]`, `/integrations`, `/integrations/[slug]`, `/glossary`, `/glossary/[slug]`, `/blog/[slug]`, `/docs`) переопределяет корневой `openGraph` из `layout.tsx` целиком, теряя `images` — Next.js не делает глубокий merge вложенных metadata-объектов. Подтверждено curl: `og:image` отсутствует в отдаваемом HTML на `/`, `/pricing`, `/features`, `/compare/obot`, `/blog/mcp-gateway-guide` (`twitter:image` при этом на месте, страдают только Slack/FB/LinkedIn/iMessage-превью). → задача #313 (high).
