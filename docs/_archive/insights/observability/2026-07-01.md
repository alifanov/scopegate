# Observability Review — 2026-07-01

**Tool:** SigNoz (self-hosted, `signoz.chatindex.app`)
**Период:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs вчера (2026-06-30) |
|---|---|---|
| Error rate | 14 ошибок (0.03%) | ↓ было 45 (0.06%) |
| p99 латентность (топ, без SSE) | 10 216 мс (threads_publish_thread) | ↑ было 7 371 мс, но в рамках 25s budget |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ было 1 (103.7 мс) — ушёл из топ-5 |
| Объём запросов (спанов) | 47 361 | ↓ было 77 450 (−39%), но +22% к 7-дневному среднему (38 660/день) |

## Findings

### Ошибки

| Маршрут | Статус | Кол-во | Комментарий |
|---|---|---|---|
| `/api/auth/[...all]` | 404 | ~4 | Низкий объём, вероятно боты/сканеры на auth-путях |
| `POST api.x.com` | 503 | 1 | Транзиентная ошибка стороннего Twitter/X API |
| `GET` (внешний) | 400 | 1 | Несущественно |
| `GET` (внешний) | 404 | 1 | Несущественно |
| `/_next/image` | 400 | 1 | Вероятно, некорректный внешний URL изображения |
| `/api/auth` | 404 | 1 | Несущественно |

**Gmail 429 больше нет** — retry с backoff (issue #159, коммиты `ed54541` + `f66e0c1`) подтверждён: за 24 ч ни одной ошибки 429 от Gmail API. Резолвит находку из снапшота 2026-06-30.

Все остальные ошибки — единичные (1-2 события), ниже порога для создания issue.

### Латентность

| Эндпоинт | p99 | Оценка |
|---|---|---|
| `GET /api/mcp/[apiKey]` | 9 678 497 мс (~2.7 ч) | ✅ SSE keep-alive — ожидаемо |
| `GET` (orphaned span) | 72 449 мс (~72 с) | ✅ OTel-артефакт — ожидаемо, признак стабилен 7+ снапшотов |
| `mcp.tool threads_publish_thread` | 10 217 мс | 🟡 Async container polling (25s budget) — рост с 7.4с, следить |
| `service-fetch threads` | 4 510 мс | 🟡 Ожидаемо для Threads async API |
| `mcp.tool linkedin_create_post` | 1 536 мс | 🟢 Стабильно, было 1.6с |
| `mcp.tool threads_get_threads` | 1 303 мс | 🟢 В рамках 30s tool timeout |
| `linkedin_create_post.prepare_image` | 800 мс | 🟢 |
| `linkedin_create_post.http_request` | 728 мс | 🟢 |
| `service-fetch linkedin` | 687 мс | 🟢 |

### База данных

Все запросы в топ-5 < 35 мс — здоровое состояние, ни один не превышает порог 100 мс (вчера был 1 запрос на 103.7 мс, сегодня ушёл из списка).

| Запрос | p99 | Оценка |
|---|---|---|
| `UPDATE ServiceConnection` | 34.5 мс | ✅ |
| `SELECT McpEndpoint` | 14.4 мс | ✅ |
| `INSERT rate_limit_bucket ON CONFLICT` | 13.5 мс | ✅ |
| `SELECT ServiceConnection` | 9.7 мс | ✅ |
| `SELECT EndpointPermission` | 8.3 мс | ✅ |

### Объём запросов

47 361 спанов за 24 ч — падение на 39% относительно вчерашнего пика (77 450, объяснён выходом новых инструментов Threads/LinkedIn/Gmail в трафик), но на 22% выше 7-дневного среднего (270 619 / 7 ≈ 38 660/день). Нормализация после разового всплеска, тренд здоровый.

### Интеграции

Threads, LinkedIn, Gmail, Google Ads, GSC — все репортируют спаны стабильно. Пробелов в трейсинге не обнаружено.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| `GET` (orphaned span, SSE-артефакт) | 7+ | Стабильно, объяснено — не требует действий |
| Gmail 429 | 1 (2026-06-30) → 0 сегодня | ✅ Резолвлено issue #159 |

## Hypotheses

- **threads_publish_thread p99 растёт** (7.4с → 10.2с за сутки) — пока в рамках 25s async-бюджета `waitForContainerReady()`. Evidence: 2026-06-30 (7 371 мс), 2026-07-01 (10 217 мс). Status: 2/3 снапшотов — не готово для issue, следить на завтра.
- **Project+endpoints aggregation query** (103.7 мс вчера) — сегодня ушла из топ-5 медленных запросов, гипотеза из прошлого снапшота не подтвердилась ростом; закрываем наблюдение.

## Recommendations

1. Без новых действий — все метрики в норме или улучшились относительно вчера.
2. Продолжить наблюдение за `threads_publish_thread` p99: если рост продолжится и в следующем снапшоте (2026-07-02) превысит ~12-15с, создать issue на UX/polling callback по правилу «3 снапшота подряд».
3. Единичные 404/400/503 на auth-путях и внешних API — ниже порога отчётности, не требуют действий.

## Новые GitHub Issues

Нет — ни одна находка не достигла порога `medium`+ по критичности.
