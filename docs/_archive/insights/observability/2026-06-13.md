# Observability Review — 2026-06-13

**Tool:** SigNoz  
**Period:** последние 24 ч

## Key Metrics

| Metric | Value (24h) | vs Jun 12 (24h) |
|---|---|---|
| Error rate | **0%** (0 / 10 731) | ↓ с 0.01% — лучший показатель |
| Request volume | 10 731 | ↓ 34% (было 16 197) |
| p99 latency (MCP SSE) | 13 155 719 мс (~3.65 ч) | ↓ с ~5.4 ч — SSE keep-alive, ожидаемо |
| p99 latency (cron refresh-tokens) | 282 мс | ~без изменений (было 304 мс) |
| p99 latency (openRouter_get_credits) | 269 мс | ↓ с 6 505 мс — вероятно, нет 7d-хвоста |
| p99 latency (refresh_connection) | 267 мс | ↓ с 304 мс |
| p99 latency (Google OAuth callback) | не в топ-10 | улучшение или нет трафика |
| Slow queries (> 100ms) | 0 | без изменений |

## Findings

### Errors

**Нулевые ошибки (наилучший снапшот за всё время наблюдения):**
- 0 ошибок из 10 731 запросов.
- HTTP-ошибки: 12 × 404 на `/` (боты/misconfigured links) + 1 × 400 на `/_next/image` + 1 × 400 на unnamed `GET` — всё незначительно.

### Latency

- `GET /api/mcp/[apiKey]` p99 = 13 155 719 мс — SSE keep-alive соединения, ожидаемо, не проблема.
- `POST /api/cron/refresh-tokens` p99 = 282 мс — в норме.
- `mcp.tool openRouter_get_credits` p99 = 269 мс — значительно лучше 7d-p99 (6 505 мс из снапшота Jun 12), возможно 24h-окно не захватило долгие хвосты.
- `GET /api/oauth/google/callback` отсутствует в топ-10 (был p99 = 1 314 мс Jun 12) — либо нет OAuth callbacks в последние 24 ч, либо улучшение после изменений.
- Все остальные span'ы < 300 мс — отличное состояние.

### DB

- `slow_db_queries` пуст — третий снапшот подряд. Prisma spans либо не коррелируют с сервисом в данной выборке, либо нет запросов > 100 мс.

### Recurring Issues (существующие, без изменений)

| Проблема | Снапшотов подряд | Issues |
|---|---|---|
| Tokens Threads в OTel spans | 4 | #108 (critical, blocked) |
| Meta Graph API 400 errors | 4 | #109 (high, blocked) |
| OpenRouter credits без кэша | 4 | #110 (medium, blocked) |
| TCP/TLS failures к сторонним API | 3+ | #107 (medium, blocked) |
| Google OAuth callback > 1 с | 2 | #111 (medium, blocked) |

Все пять issues остаются в статусе `status:blocked`. Без разблокировки новые issues не создаются.

## Hypotheses

- **Снижение объёма запросов на 34%** (10 731 vs 16 197) — может быть нормальной суточной вариацией или снижением активности пользователей. Если сохранится 2+ снапшота подряд — создать issue для диагностики.
- **Google OAuth callback исчез из топ-10** — возможно, улучшение в рамках OAuth flow (изменения в `oauth-flow.ts` из git status) или просто нет OAuth-трафика. Стоит отслеживать завтра.
- **Unnamed `GET` span с p99 13 132 445 мс** — второй SSE-подобный span без `http.route`. Вероятно, дублирующий span MCP SSE. Стоит проверить, корректно ли устанавливается `http.route` в этом span.

## Recommendations

1. **#108 (critical, blocked)** — Снять блокировку. Добавить `SpanProcessor` для редактирования `access_token=...` из `http.url`. 4-й снапшот подряд — токены Threads утекают в SigNoz.
2. **#109 (high, blocked)** — Снять блокировку. Meta Graph API 400 продолжаются; расследовать коды 190/102 → `OAuthTokenError`.
3. **#111 (medium, blocked)** — Снять блокировку. Профилировать `handleOAuthCallback` в `oauth-flow.ts` (файл изменён в текущей ветке — проверить, не решена ли проблема).
4. Unnamed `GET` span — проверить `trace.getActiveSpan()?.setAttribute('http.route', ...)` для всех SSE handlers.
