# Observability Review — 2026-06-20

**Tool:** SigNoz (self-hosted, signoz.chatindex.app)
**Period:** последние 7 дней (168 ч) | Сервис: `scopegate`

## Key Metrics

| Metric | Value | vs предыдущий период |
|---|---|---|
| Всего запросов | 178 752 | +1 201 (↑0.7%) |
| Ошибок (has_error=true) | 256 | = без изменений |
| Error rate | 0.14% | ✅ низкий |
| p99 (SSE keep-alive) | 16 521 199 мс¹ | = без изменений |
| p99 (threads_publish_thread) | 18 650 мс | = без изменений vs 2026-06-19 |
| Медленных DB-запросов (>50 мс) | 3 | = без изменений |
| HTTP 500 на /api/mcp/[apiKey] | 34 | = без изменений (#140 открыт) |
| HTTP 500 на /api/auth/[...all] | 4 | = повторяется 2+ снапшота → новый issue #142 |

¹ SSE стриминг держит соединение открытым часами — ожидаемое поведение.

## Findings

### Ошибки

- **34 HTTP 500 на `/api/mcp/[apiKey]`** — сохраняется, issue #140 открыт (priority:high, needs-human).
- **4 HTTP 500 на `/api/auth/[...all]`** — повторяется второй снапшот подряд, создан issue #142 (priority:high).
- **3 HTTP 401 на `/api/auth/[...all]`** — норма для истёкших сессий; мониторинг.
- **27 HTTP 404 на `/`** — боты/сканеры; норма.
- **4 HTTP 400 на `/[blocked-action]`** — middleware работает корректно.
- **4 HTTP 404 на `/apple-touch-icon-precomposed.png`** — браузерный auto-fetch; норма.

### Латентность

- **`mcp.tool threads_publish_thread` p99 = 18 650 мс** — без изменений vs вчера (регрессия с 11 900 мс сохраняется). Issue #122 обновлён комментарием.
- **`service-fetch threads` p99 = 10 626 мс** — Threads API медленный по-прежнему.
- **`mcp.tool linkedin_create_post` p99 = 2 008 мс** — на уровне вчерашнего снапшота; issue #141 открыт.
- **`GET /api/oauth/threads/callback` p99 = 1 506 мс** — повторяется второй снапшот; создан issue #143 (priority:medium).
- **`mcp.tool twitter_post_tweet` p99 = 926 мс** — приемлемо (<1 с), мониторинг.

### Database

- **`SELECT Project` с агрегацией McpEndpoint p99 = 90 мс** — повторяется; `@@index([projectId])` уже есть на `McpEndpoint`. Замедление из-за COUNT-агрегации; индекс не поможет. Без явной деградации — гипотеза.
- **`INSERT INTO AuditLog` p99 = 53 мс** — стабильно, ожидаемо.
- **`SELECT McpEndpoint` p99 = 50 мс** — покрыто индексом.

### Интеграции

- Все сервисы передают трейсы — пробелов нет.
- Error log samples: пусто — ERROR-уровень в логах не активен или событий не было.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| threads_publish_thread p99 >11 с | 3+ | #122 open, needs-human |
| HTTP 500 на /api/mcp/[apiKey] — 34 шт. | 2+ | #140 open, needs-human |
| HTTP 500 на /api/auth/[...all] — 4 шт. | 2 | #142 создан сегодня |
| Threads OAuth callback p99 >1.5 с | 2 | #143 создан сегодня |

## Hypotheses

- **AuditLog рост таблицы** — INSERT 53 мс; без retention policy деградирует в течение нескольких месяцев при текущем объёме.
- **`twitter_post_tweet` 926 мс** — стабильно ~1 с; если перейдёт за 1 с — потребует issue.
- **Project COUNT-агрегация 90 мс** — при росте числа McpEndpoint per Project возможно ухудшение; рассмотреть денормализацию счётчика в Project.

## Recommendations

1. **Issue #142 (high, новый)** — Расследовать 500 на `/api/auth/[...all]`: открыть SigNoz, фильтр `http.route=/api/auth/[...all], status=500`, изучить трейсы.
2. **Issue #143 (medium, новый)** — Threads OAuth callback 1.5 с: проверить применение timeout 8 с к token exchange; добавить диагностику span.
3. **Issue #140 (high, открыт)** — 34 MCP 500 без изменений: требует ручного расследования трейсов.
4. **Issue #122 (high, обновлён)** — threads_publish_thread регрессировал с 11.9 с до 18.6 с; приоритизировать.
5. **AuditLog retention** — запланировать удаление записей старше 90 дней до деградации INSERT.
