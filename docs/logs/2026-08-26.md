## Performance

**Tool:** SigNoz
**Период:** 2026-08-25 06:00 → 2026-08-26 06:00 UTC (24ч), сервисы `scopegate-cloud` и `scopegate-selfhost`

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 36088 | 40498 |
| Ошибок | 0 (0.00%) | 13 (0.03%) |
| p99 латентность (топ, кроме известных паттернов) | `GET /signup` 585.7мс | SSE keep-alive / Meta-polling эндпоинты — не регрессия (см. Findings) |
| Медленные DB-запросы (>100мс) | 0 — все ≤11.5мс | 0 — все ≤68.6мс |
| Запросов vs вчера | 36088 vs 31517 (+14.5%) | 40498 vs 51506 (−21.4%) |

### Findings

Чисто — ни одного нового дефекта, все наблюдения укладываются в уже задокументированные паттерны.

- **H-006 закрыта**: `McpEndpoint` SELECT (резолв apiKey на MCP hot path) p99 = 68.6мс на `scopegate-selfhost` — ниже порога 200мс и близко к до-регрессионному базлайну (≤48.2мс, 08-11…08-22). Подтверждает, что индекс `TeamMember(projectId, role)` (Task #285, коммит `8ed262e`) устранил регрессию, зафиксированную 08-24/08-25 (242.7мс → 249мс).
- **Errors**: 13 ошибок на `scopegate-selfhost` — все известные `OAuthTokenError` (revoked-коннекты `googleAds`, `linkedin`: истёкший/отсутствующий refresh-токен) плюс один Threads timeout (>3500мс, укладывается в retry-логику). Не дефект.
- **GoogleAds 403/400** на `googleAds:searchStream` (4 вхождения) — тот же эндпоинт, для которого сегодня уже вышел фикс текста ошибки (коммит `0031917`, "вернуть текст ошибки searchStream и починить два разрезных отчёта"); окно снапшота (до 06:00 UTC) могло частично захватить период до деплоя фикса — следующий снапшот покажет, ушли ли ошибки.
- **Latency**: `GET /api/mcp/[apiKey]` p99 ≈176 мин на `scopegate-selfhost` — артефакт долгоживущего SSE-соединения с keep-alive пингами каждые 30с, тот же паттерн, что и во всех прошлых снапшотах, не инцидент. `mcp.tool threads_publish_thread` (21.6с) и `instagram_create_post` (20.9с) — ожидаемо, укладываются в 25с-бюджет `waitForContainerReady()`.
- **Integrations**: оба сервиса отдают трейсы без пропусков; DB-инструментация (Prisma) работает на обоих.

### Recurring Issues

—

### Hypotheses

H-006 закрыта — см. `docs/state/hypotheses.md`.
