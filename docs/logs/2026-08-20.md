## Performance

**Tool:** SigNoz
**Период:** 2026-08-19 10:00 → 2026-08-20 10:00 UTC (24ч), сервисы `scopegate-cloud` и `scopegate-selfhost`

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 28733 (вчера 32911) | 47590 (вчера 47913) |
| Ошибок | 0 (0.00%) | 6 (0.01%) |
| p99 латентность (топ, кроме известных паттернов) | `POST /` 147.4мс | `GET /api/mcp/[apiKey]` — SSE keep-alive, не регрессия |
| Медленные DB-запросы (>100мс) | 0 — все ≤11.8мс | 0 — все ≤40.6мс |

### Findings

Чисто — ошибок почти нет на обоих сервисах, все DB-запросы в норме, ни один пользовательский эндпоинт не превышает порог 1с.

- `mcp.tool searchConsole_inspect_url` p99 6623.1мс на `scopegate-selfhost` — тот же паттерн, что закрыт как H-001 (confirmed/external, GSC API намеренно на таймауте 30с), фикса не требует.
- `GET /api/mcp/[apiKey]` p99-артефакт на `scopegate-selfhost` — долгоживущее SSE-соединение (keep-alive пинги каждые 30с), не регрессия.
- `/api/mcp/[apiKey]` 400×60 на `scopegate-selfhost` — тот же паттерн, что закрыт как non-defect (H-002, `server/discover`-пробы клиентов Claude Code).
- Google Ads: `Google Ads query error (400)` ×2 в логах `scopegate-selfhost` — revoked-соединение, уведомление владельцу проекта уже отправлено штатным reconnect-флоу, не задача агента (тот же паттерн, что 2026-08-13).
- 400/401/404 шум (`/wp-admin/`, `/ops.php`, `/xmlrpc.php`, `/api/mcp/[apiKey]` 401) на обоих сервисах — бот-сканирование и неавторизованные пробы ключа, уже рейт-лимитится, не дефект.

### Recurring Issues

—

### Hypotheses

—

### Recommendations

—
