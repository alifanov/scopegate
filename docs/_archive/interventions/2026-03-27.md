# Analytics Report — ScopeGate — 2026-03-27

> **Context**: День 24 после Hacker News Show HN (2026-03-03). Предыдущий отчёт: 2026-03-24.
> **⚠️ ВАЖНО**: PostHog MCP подключён к другому аккаунту (SecScanner). Проект ScopeGate (ID 132140) недоступен через текущий MCP. `POSTHOG_PERSONAL_API_KEY` в `.env` возвращает 401. Данные ниже — из отчёта March 24 + анализ кода. Новых live-данных за последние 3 дня нет.

---

## ⚠️ Проблема с PostHog MCP — нужно исправить

**Симптомы:**
- `mcp__plugin_posthog__projects-get` показывает только SecScanner/Adbrief/QaBot
- `switch-project(132140)` → 404 Not Found
- `POSTHOG_PERSONAL_API_KEY=phx_QwiKi8...` в `.env` → 401 Invalid

**Как исправить:**
1. Открыть терминал и запустить `/mcp` в Claude Code
2. Переаутентифицировать PostHog MCP через браузер, войдя в аккаунт ScopeGate (eu.posthog.com)
3. Или создать новый личный API ключ: eu.posthog.com → Settings → Personal API Keys → обновить `.env`

---

## Key Metrics (Last 30 days) — данные на 24 марта

| Metric | Value |
|---|---|
| MAU | **61** |
| WAU | **1** |
| DAU | **0** (11 дней подряд нулевого трафика) |
| Новые регистрации | **9** |
| Логины после регистрации | **1** (11.1%) |
| CTA клики (`landing_cta_clicked`) | **7** (все до OSS-пивота) |
| Pageviews | **68** |
| Ошибки ($exception) | **0** ✅ |

---

## Изменения за последние 3 дня (24 → 27 марта)

| Что | Статус |
|---|---|
| Новые коммиты | **0** — кодовая база не изменилась |
| `github_cta_clicked` трекинг | ❌ Не добавлен (слепая зона сохраняется) |
| 48h drip email | ❌ Не реализован |
| Twitter/X thread | ❌ Не запущен |
| Reddit-дистрибуция | ❌ Не сделана |
| GSC indexing requests | ❌ Не сделано |
| Личное письмо logicswarm.com | ❌ **Дедлайн 2026-03-26 истёк** |

---

## Recurring Issues

| Проблема | Дней | Δ от 24 марта |
|---|---|---|
| 0 MCP Endpoints / ServiceConnections | **24** | +3 🔴 |
| Нулевой трафик (DAU=0) | **14 дней подряд** | +3 🔴 |
| `github_cta_clicked` не трекается | **4** | +3 🔴 |
| 48h drip email не реализован | **12+** | +3 🟡 |
| Twitter/X thread не запущен | **12+** | +3 🟡 |
| Reddit-дистрибуция не сделана | **7+** | +3 🟡 |
| GSC indexing requests | **12+** | +3 🟡 |
| PostHog MCP не настроен для ScopeGate | **NEW** 🔴 |

---

## 5 Recommendations

### 1. Восстановить PostHog MCP доступ — Priority: **Critical**

- **Metric**: Без PostHog мы слепы ко всем метрикам. Невозможно принимать решения на основе данных.
- **Action**:
  1. `claude /mcp` → Reconnect PostHog → войти в eu.posthog.com аккаунт ScopeGate
  2. Или: eu.posthog.com → User → Personal API Keys → создать новый ключ → обновить `POSTHOG_PERSONAL_API_KEY` в `.env`
- **Impact**: Восстановить аналитику для всех будущих отчётов.

---

### 2. Добавить `github_cta_clicked` трекинг — Priority: **Critical**

- **Metric**: С March 24 мы не знаем сколько людей кликает на GitHub CTA. 14+ дней нулевого трафика без возможности измерить конверсию главного CTA.
- **Action**: В `hero.tsx`, `cta-section.tsx`, `navbar.tsx` добавить:
  ```tsx
  import posthog from 'posthog-js'
  // на onClick:
  posthog.capture('github_cta_clicked', { source: 'hero' | 'navbar' | 'cta_section' })
  ```
- **Impact**: Восстановить видимость главной воронки после OSS-пивота.

---

### 3. Запустить "We open-sourced ScopeGate" Tweet + Reddit — Priority: **Critical**

- **Metric**: 14 дней нулевого трафика. OSS-пивот (March 23) создаёт новостной повод, который ещё не использован.
- **Action**:
  - Tweet: "We just open-sourced ScopeGate — permission gateway for AI agents. Per-agent MCP scopes, no more full OAuth. MIT, self-hostable. ⭐ github.com/alifanov/scopegate"
  - Reddit: r/ClaudeAI, r/mcp, r/selfhosted
  - Новый Show HN: "Show HN: ScopeGate is now open-source — granular permission gateway for AI agents (MCP)"
- **Impact**: 50–300 уников за 48 часов.

---

### 4. Реализовать 48h activation drip email — Priority: **High**

- **Metric**: 9 регистраций, 0 ServiceConnections, 0 автоматических follow-up.
- **Action**: `src/app/api/cron/activation-drip/route.ts`:
  - Prisma: найти пользователей без ServiceConnection за 48–72h
  - Отправить через Resend: "Still setting up? Here's how — cloud vs self-hosted"
  - Launchd plist: ежедневно 9:00
- **Impact**: Автоматический re-engagement для всех будущих пользователей.

---

### 5. Добавить GitHub repo в .env.example и README — Priority: **Medium**

- **Metric**: OSS-пивот активен, но ни `.env.example`, ни README не упоминают self-hosted setup. Новые посетители GitHub не понимают как начать.
- **Action**: Обновить README.md с секцией "Quick Start (self-hosted)" + ссылками на документацию.
- **Impact**: Улучшить GitHub → deployment conversion для новых OSS пользователей.

---

## Технический статус кодовой базы

| Фича | Статус |
|---|---|
| Welcome email (`user_registered`) | ✅ Реализован |
| PostHog identity merge (opt_in на signup) | ✅ Реализован |
| Blog post least-privilege | ✅ Добавлен |
| OnboardingBanner | ✅ Реализован |
| Post-signup redirect → /projects?welcome=1 | ✅ Реализован |
| Mobile onboarding UX | ✅ Реализован |
| OSS pivot — все CTA → GitHub | ✅ Реализован |
| `github_cta_clicked` PostHog трекинг | ❌ Нет |
| 48h activation drip email | ❌ Нет |
| Twitter/X thread | ❌ Не запущен |
| Reddit-дистрибуция | ❌ Нет |
| GSC indexing requests | ❌ Нет |
| PostHog MCP настроен для ScopeGate | ❌ Сломан |

---

## Summary

ScopeGate — день 24. **14 дней подряд нулевого трафика.** Нет новых данных из PostHog (MCP сломан). Нет новых коммитов за 3 дня. Все ключевые рекомендации из прошлого отчёта остаются нереализованными.

**3 немедленных действия:**
1. 🔴 Починить PostHog MCP — без этого аналитика слепа
2. 🔴 Добавить `github_cta_clicked` — без этого главная воронка не измеряется
3. 🔴 Запустить OSS-объявление в Twitter + Reddit — 14 дней тишины, есть свежий повод
