# 2026-09-04

## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-03 06:00 → 2026-09-04 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 30 864 | 43 824 |
| Ошибок (error rate) | 0 (0%) | 7 (0.02%) |
| p99 самого медленного эндпоинта | `GET /og-image.png` 52.2ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (держится часами, keep-alive каждые 30с), реальный «медленный» — `mcp.tool instagram_create_post` 20.1s |
| Медленные DB-запросы (>100мс) | 0 (макс. 14.8ms) | 1 — `McpEndpoint` lookup 326.9ms |
| Запросов за 7 дней | 261 407 (~37.3к/сут) | 328 466 (~46.9к/сут) |
| Объём за 24ч vs 7-дн. среднее | −17.3% | −6.6% (в норме, тот же диапазон, что и 08-25) |

### Findings

- **[DB] `McpEndpoint` lookup p99 326.9ms на `scopegate-selfhost`** — выше порога 200мс. Тот же паттерн, что H-006 (закрыта 08-26 индексом `TeamMember(projectId, role)`, коммит `8ed262e`): регрессия вернулась разово 09-01 (259ms), затем 2 чистых прогона (09-02, 09-03) без неё, сегодня снова выше порога. Streak сброшен чистыми прогонами → считается новым наблюдением, не тем же H-006. Заведена **H-007** (1/3), задачу не создавал — ниже порога 3 прогонов/2 источников.
- **Errors**: 7 ошибок на `scopegate-selfhost` — Threads timeout (retry-логика) и повторный `googleAds` `invalid_grant` (уже revoked-коннект, штатное поведение `classifyOAuthError`). Не дефект.
- Боевой (404/400) шум на обоих сервисах — сканирование ботами (`.well-known/*`, `wp-json`-подобные пути) и попытки подбора MCP API-ключей; уже задокументированный паттерн, оба пути защищены.
- `mcp.tool instagram_create_post` (20.1s) / `threads_publish_thread` (20.0s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- **H-007** (новая) — `McpEndpoint` lookup DB-latency регрессия вернулась после фикса H-006; 1/3, `tracking`. Ледджер: `docs/state/hypotheses.md`.

### Recommendations

1. Следить за `McpEndpoint` lookup p99 ещё 2 прогона — если попадёт в диапазон >200мс 3 раза подряд, искать причину заново (например, рост числа `TeamMember` на проект делает индекс менее эффективным) и заводить задачу.
