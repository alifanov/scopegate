## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-04 06:00 → 2026-09-05 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 33 071 | 36 537 |
| Ошибок (error rate) | 0 (0%) | 3 (0.01%) |
| p99 самого медленного эндпоинта | `GET /scopegate-demo.mp4` 723.3ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (держится часами, keep-alive каждые 30с), реальный «медленный» — `mcp.tool instagram_create_post` 20.5s |
| Медленные DB-запросы (>100мс) | 0 (макс. 14.4ms) | 1 — `McpEndpoint` lookup 109.3ms (ниже порога 200мс) |
| Запросов за 7 дней | 260 446 (~37.2к/сут) | 317 464 (~45.4к/сут) |
| Объём за 24ч vs 7-дн. среднее | −11.1% (в норме) | −19.4% (в норме, тот же диапазон, что 08-25/08-19) |

### Findings

- **[DB] `McpEndpoint` lookup** вернулась к норме — 109.3ms, ниже порога 200мс. H-007 (регрессия из 09-04, 326.9ms) снова разорвана чистым прогоном — тот же on/off паттерн, что и после блипа 09-01. Задачу не заводили.
- **Errors**: 3 ошибки на `scopegate-selfhost` — все от одного revoked-коннекта `searchConsole` (`cmlqlb01k000h0jlh8momegho`, `invalid_grant`, штатное поведение `classifyOAuthError`, уведомление о реконнекте уже отправлено). Не дефект.
- `mcp.tool instagram_create_post` (20.5s) / `threads_publish_thread` (19.6s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.
- Боевой (404/400/401) шум на обоих сервисах — сканирование ботами (`.git/config`, `wp-json`, `.well-known/*`) и попытки подбора MCP API-ключей на `scopegate-cloud` (448 x 401 на `/api/mcp/[apiKey]`); оба пути уже защищены (brute-force rate-limit в `api-keys.ts`, fail-open rate limiter).

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- H-007 — `McpEndpoint` lookup latency: streak снова разорвана чистым прогоном (109.3ms). Ледджер: `docs/state/hypotheses.md`.

### Recommendations

1. Продолжать наблюдение за `McpEndpoint` lookup — паттерн «вспышка → 2 чистых прогона» повторился дважды (09-01, 09-04); если разовые вспышки участятся, разбирать нагрузочный профиль отдельно от порога «3 прогона подряд».

## Design

**Scope:** прод `https://scopegate.dev` (`stagingUrl` не задан), headless Chromium arm64, вьюпорты 320×720, 390×844 и 1440×900. Страницы из `sitemap.xml`: `/`, `/features`, `/pricing`, `/docs`, `/compare`, `/integrations`, `/glossary`, `/blog`, `/compare/composio`, `/integrations/gmail`, `/glossary/mcp-gateway`, `/blog/mcp-gateway-guide` — плюс связанные из футера `/privacy`, `/terms`, `/cookies`.

### Quality

| Dimension | Finding | Severity | Page / Component |
|---|---|---|---|
| перекрытие | FAB `mailto` (`fixed bottom-5 right-5`, 322–370 px по X) закрывает хвосты строк: `/docs` 4/5 позиций прокрутки, `/blog/mcp-gateway-guide` 5/5 | high | `src/app/layout.tsx:71` (#302) |
| дублирование | `<h1>` и `<h2>` на `/features` совпадают дословно — «The missing permission layer / for the MCP ecosystem» | medium | `features/page.tsx:143` + `landing/features.tsx:66` (#305) |
| перекрытие | Бейдж «0 security incidents» наезжает на шапку терминала — закрыты точки и половина `agent-setup.ts`, на 390 и 1440 | medium | `landing/hero.tsx:203` (#306) |
| консистентность | Иконки сервисов на `/integrations` и `/integrations/*` — системные эмодзи, весь остальной UI на собственных SVG | medium | `/integrations` (#308) |
| контраст | `text-slate-600` 2.66:1, `text-violet-600` 3.31–3.42:1, `text-slate-500` 4.00–4.23:1 при пороге 4.5:1 | medium | 8 файлов (#307) |
| overflow | На 320 px обрезанных элементов нет; таблицы `/features` и блоки `<pre>` на `/docs` прокручиваются по горизонтали (`overflow-x-auto`) — это намеренно | — | — |

### Performance

| Metric | Current | Target | Page | Task |
|---|---|---|---|---|
| LCP (390×844, cold) | 7880 / 8024 / 8144 / 8836 / 9828 мс (5/5) | < 2.5s | `/` | #303 |
| LCP (1440×900, cold) | 2844 мс | < 2.5s | `/` | покрыто #303 |
| LCP (390×844, cold) | 1752 мс | < 2.5s | `/pricing` | — |
| LCP (390×844, cold) | 1868 мс | < 2.5s | `/docs` | — |
| CLS (390×844, cold) | 0.193 (3/4 прогона) | < 0.1 | `/` | #304 |
| CLS (1440×900, cold) | 0.094 | < 0.1 | `/` | — |
| CLS | 0 | < 0.1 | `/pricing`, `/docs`, `/features` | — |
| TBT | 4901 мс, длинные задачи 342/905/385 мс | < 200ms | `/` | контекст в #303 |
| `domInteractive` | 4615 мс при TTFB 135 мс и всех 19 ресурсах, загруженных к 484 мс | — | `/` | контекст в #303 |
| Транспорт | 257 КБ (158 КБ JS), `scopegate-demo.mp4` больше не тянется — только `scopegate-demo-poster.webp` 6 КБ | — | `/` | #262 держится |

### Production readiness

| Category | Component / Page | Gap | Risk |
|---|---|---|---|
| overflow | все 12 страниц, 320 px | `document.scrollWidth == 320` везде; обрезанных текстовых блоков нет | — |
| contrast | `/privacy`, `/terms`, `/cookies` | 25 / 27 / 12 текстовых узлов ниже AA — это корпус юридического текста, а не подписи | нечитаемые условия и политика |
| contrast | `/features`, `/compare/*`, `/`, `/pricing`, `/blog*` | подписи карточек, даты, хлебные крошки и три ссылки-акцента ниже AA | — |
| tap target | футер, 390 px | иконки GitHub/X ≥ 24×24 — фикс #265 держится; текстовые ссылки 17 px при шаге 27 px | на грани |
| i18n | — | продукт англоязычный, непереведённых строк и плейсхолдеров не найдено | — |
| truncation | `/integrations`, `/blog`, `/glossary` | `line-clamp` на карточках — намеренный дизайн | — |

### Recurring

- **Перекрытие FAB** — третий прогон подряд (2026-08-15 #242, 2026-08-22 #259, сегодня #302). Оба прошлых фикса были вертикальным сдвигом одной и той же кнопки: `bottom-5` → `bottom-24` → `bottom-5`. #302 сформулирована как структурный фикс (скрыть на мобильных, ссылку — в футер), чтобы не запускать четвёртый круг.
- **CLS на `/`** — третий прогон подряд (0.076 → #244, 0.314 → #260, сейчас 0.193). Порог < 0.1 не достигнут ни разу; источник каждый раз новый, сейчас — процентное позиционирование декоративных blur-кругов.
- **Дублирующийся заголовок** — второй прогон подряд, но на другой странице: `/pricing` (#261, закрыт) → `/features` (#305). Готовый паттерн `showHeading` из `pricing.tsx:97` не был применён ко второму такому же месту.

Замер контраста в прошлых прогонах давал ложную чистоту: Tailwind v4 отдаёт цвета как `lab()`/`oklch()`, и парсер по `rgb()` их не разбирал, подставляя белый фон по умолчанию. В этом прогоне цвета нормализованы через канвас — отсюда находки, которых «не было» 2026-08-22.

### Recommendations

- `src/app/layout.tsx:71` → `hidden md:flex` + `mailto` в футере → на 320–414 px ни одного пересечения кнопки с текстовыми прямоугольниками (#302).
- `src/components/landing/hero.tsx:63` → `useState(CODE_LINES.length)` и запуск печати только после монтирования → LCP на `/` < 2.5 с, строки `CODE_LINES` есть в HTML-ответе без `invisible` (#303).
- `src/components/landing/hero.tsx:82,84` → фиксированные смещения вместо `top-1/3` / `bottom-1/4` → CLS < 0.1 на 390 и 1440 (#304).
- `src/components/landing/features.tsx:57` → проп `showHeading`, `showHeading={false}` из `features/page.tsx:163` → «The missing permission layer» ровно один раз в DOM (#305).
- `src/components/landing/hero.tsx:203` → `-top-8` → бейдж не пересекается с шапкой терминала (#306).
- `text-slate-600` → `text-slate-400`, `text-violet-600` → `text-violet-400`, корпус юридических страниц `text-slate-500` → `text-slate-400` → ноль узлов ниже 4.5:1 (#307).
- `/integrations` → векторные логотипы вместо эмодзи (#308).
- Полировка, без задач: на `/pricing` между подзаголовком и тумблером Monthly/Annual ~150 px пустоты на 1440; на `/compare/*` левый край таблицы (x=232) не совпадает с левым краем текста hero (x=296); в карточке Free название плана и цена — одно и то же слово «Free».

## UX

**Walked:** https://scopegate.dev · mobile 390×844, desktop 1440×900 · залогинен как владелец (Google SSO)

### Flows

| Flow | Mobile | Desktop | Where it broke |
|---|---|---|---|
| Лендинг → /pricing → /signup | pass | pass | — |
| Вход: password / magic-link / invite | pass | pass | — |
| Денежный путь: Annual → Upgrade to Pro | **fail** | **fail** | /billing: кнопка виснет на «Opening…» бесконечно, без ошибки и без навигации (#309) |
| Денежный путь: Monthly → Upgrade to Pro | pass | pass | редирект на polar.sh/checkout ~7 c; спиннера на время ожидания нет |
| Первый запуск проекта: пустой проект → Connect Service | pass | pass | — |
| Настройки аккаунта и админка | friction | friction | /settings дублирует таблицу пользователей из /admin/users, без «Invite Link» (#311) |

### States checked

| Screen | Loading | Empty | Error |
|---|---|---|---|
| /signup | n/a | n/a | ok (native required + email) |
| /login (пароль) | ok | n/a | ok — «Invalid email or password» |
| /magic-link?token=bad | ok | n/a | ok — редирект на /login?error=INVALID_TOKEN с текстом |
| /invite/[bad token] | ok | n/a | ok — «invalid or already used» + «Back to sign in» |
| Проект → Endpoints | ok | ok — «Connect a service first, then create an endpoint», «New Endpoint» disabled | n/a |
| Проект → Connections | ok | ok — «No services connected» | n/a |
| Проект → Logs | ok | ok — «No audit logs yet» | n/a |
| /notifications | ok | ok — «You're all caught up» | n/a |
| /billing (checkout) | **missing** — «Opening…» без таймаута и без тоста | n/a | **missing** — ошибка чекаута не показывается вообще |

### Not walked

- Оплата на Polar checkout — реальное списание. Дошёл до формы оплаты (месячный тариф, $29/mo), дальше не пошёл.
- Подключение сервиса по OAuth и создание MCP-эндпоинта — записали бы реальный `ServiceConnection`/`McpEndpoint` в прод. Диалог «Connect a Service» открыт и проверен (27 сервисов, поиск, кнопки подключения видны на обоих вьюпортах), сам OAuth не запускался.
- Регистрация нового аккаунта через magic-link — отправка реального письма и создание пользователя в проде.
- «Invite Link» на /admin/users, удаление проекта, Downgrade to Free — запись/деструктив.

### Scored critique (Nielsen, 0–4)

| Heuristic | Score | Note |
|---|---|---|
| 1. Видимость статуса | 2 | «Opening…» без таймаута; месячный чекаут думает ~7 c без индикатора прогресса |
| 2. Соответствие реальному миру | 3 | язык дев-аудитории, терминология понятна |
| 3. Контроль и свобода | 3 | выходы есть везде, кроме зависшего чекаута — только перезагрузка |
| 4. Консистентность | 2 | «Connect a service» / «Connect Service» / «Connections» / «No services connected»; /settings дублирует /admin/users |
| 5. Предотвращение ошибок | 2 | кнопка годового плана активна, хотя купить нельзя; required-поля и disabled «New Endpoint» — хорошо |
| 6. Узнавание вместо припоминания | 3 | сайдбар, хлебные крошки, TOC в /docs |
| 7. Гибкость и эффективность | 3 | поиск в диалоге сервисов, фильтры в логах |
| 8. Минимализм | 3 | чисто; известные проблемы лендинга — в задачах #303–#307 |
| 9. Диагностика ошибок | 3 | логин/magic-link/invite объясняют причину; чекаут молчит |
| 10. Помощь и документация | 3 | /docs даёт квикстарт, конфиг MCP-клиента и self-host |
| **Итого** | **27/40 (68%)** | |

### Recurring

Первый прогон check-ux после закрытия #266–#273 — регрессий из прошлых прогонов не найдено, все прежние находки закрыты. Годовой чекаут (#267 закрыт) сломан снова, но в другом месте: раньше не было годового варианта вообще, теперь он есть в UI и не доводится до Polar — заведён заново как #309.

### Recommendations

- Проект → одно название для одной сущности: сейчас на соседних экранах «Connect a service», «Connect Service», вкладка «Connections» и пустое состояние «No services connected». Критерий: во всех четырёх местах используется одно слово.
- Дашборд на 390px → кнопка «Open sidebar» 28×28 — единственный способ добраться до навигации на мобильном. Критерий: область нажатия ≥44×44 (иконка может остаться 28px).
- /projects (redirect) → при переходе на /login?redirect=/projects нет пояснения, зачем пользователя перебросили. Критерий: на форме входа показывается строка «Sign in to continue».
- /docs на 390px → блоки кода прокручиваются по горизонтали, но нет кнопки «Copy». Критерий: конфиг MCP-клиента копируется одним нажатием.
