# Web Vitals — 2026-07-27

**Target:** https://scopegate.dev  ·  **Strategy:** mobile  ·  **Source:** lab (local Lighthouse)

## Metrics
| Time | LCP | TBT | CLS | TTFB | FCP | Score | Result | Task |
|---|---|---|---|---|---|---|---|---|
| 17:26 | 24.6s | 93.1s | 0.156 | 38ms | 1.8s | 28 | poor | #198 |

## Notes
Первый прогон на этом проекте. LCP и TBT — poor (LCP >4s, TBT на два порядка выше 600ms).
CLS — needs-improvement (0.156), FCP и TTFB — good.

Прогон выдал предупреждение "tested device appears to have a slower CPU than Lighthouse
expects" (throttling может искажать TBT/LCP вверх). Контрольный прогон без симуляции CPU
throttling (`--throttling-method=provided`) всё равно показал серьёзную проблему:
mainthread-work-breakdown 22.8s, bootup-time 9.9s, LCP 16.1s, CLS 0.292, TTI не вычисляется
(`NO_TTI_CPU_IDLE_PERIOD` — главный поток не выходит в простой). Значит проблема реальная,
не только артефакт throttling-симуляции на медленной машине.

`long-tasks` audit указывает на один и тот же JS-чанк (`619a8348d835fe12.js`),
многократно блокирующий главный поток (10s/7.8s/6.6s/4.7s/3.4s за прогон) при общем
весе страницы всего 337 KiB — подозрение на дорогое повторное выполнение/гидратацию,
не на размер ассетов. См. task #198.

Нет предыдущих снапшотов для сравнения тренда — это первая точка отсчёта.
