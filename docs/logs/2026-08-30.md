# 2026-08-30

## Security

**Period:** 2026-08-23 — 2026-08-30 (с прошлого прогона security-audit)

### Findings

| Category | Finding | Severity | File / Config | Source |
|---|---|---|---|---|
| Broken access control / client-side-only check | `createOAuthStartRoute()` берёт `client_id` из env вместо `resolveOAuthApp()` — BYO-креды не работают у 9 провайдеров, а cloud-политика «только своё приложение» держится лишь на клиентском `getConnectTarget()` | high | `src/lib/oauth-start-route.ts:19`, обход `src/lib/provider-registry.ts:1154` | code-review |
| Robustness (low, задача не заводилась) | `POST /api/projects/[projectId]/oauth-apps`: `input.clientId.trim()` до проверки типа → 500 на нестроковом теле | low | `src/lib/oauth-app-credentials.ts:44` | code-review |

Dependabot — 0 открытых алёртов, локальный `pnpm audit` чист. Code scanning (404 «no analysis found») и secret scanning (выключен) на репозитории недоступны — с этих поверхностей находок нет.

### Recurring Issues

Нет. Обе находки прошлого аудита закрыты и перепроверены: лимит памяти `invalidKeyBuckets` + последний хоп XFF на месте (#275, `src/lib/mcp/api-keys.ts`), `deepmerge-ts` держится на `^8.0.0` через `pnpm.overrides` (#276). Регрессий SSRF/DNS-rebinding, path traversal в путях MCP-инструментов и open-redirect в magic-link не обнаружено — `originCheck` better-auth покрывает `callbackURL` на `/api/auth/magic-link/verify`.

### Recommendations

1. **OAuth start-роут** → резолвить креды через `resolveOAuthApp(providerKey, projectId)` внутри `buildUrl`, `handleOAuthStart` уже превращает бросок в 428 → тест: проект со своим `ProviderCredential` получает в authorize-URL свой `client_id`; в cloud без приложения роут отдаёт 428, а не редирект на consent оператора. Задача #288.

## Changes

- Task #288 — `createOAuthStartRoute` резолвит `client_id` через `resolveOAuthApp(config.stateProvider, projectId)` вместо `process.env[clientIdEnv]`; BYO-креды теперь работают для всех 9 провайдеров, cloud без своего приложения получает 428 вместо редиректа на consent оператора (`src/lib/oauth-start-route.ts`, тесты в `src/lib/__tests__/oauth-start-route.test.ts`)
