# 2026-08-12

## Changes

- Task #210 — nanoid 3.3.16→6.0.1 через pnpm.overrides (GHSA-2v37-7h3g-55p8, DoS при size=0) (commit TBD)
- Task #211 — ip-address 10.2.0→10.5.0 через pnpm.overrides (GHSA-mwp4-54f8-5fhr, GHSA-4xrf-jv44-h6hh, GHSA-22jq-vg5j-6vgg — SSRF-обход через leading-zero/CIDR/NAT64) (commit TBD)
- Task #213 — fast-uri 4.1.1→4.1.2 через pnpm.overrides (GHSA-7p8r-x3mc-p8w7 — host confusion через обратный слэш в authority) (535d6f8)
- Task #214 — hono ~4.12.32→^4.13.1 прямая зависимость + выровнен override (GHSA-f23p-vx2j-j53r утечка SSR-вывода между пользователями, GHSA-54fx-42gc-7vw4 DoS в Language middleware, GHSA-8j4g-w8fx-2239 ReDoS в CORS middleware, GHSA-79qm-7rj5-m7r9 Proxy Helper не чистит Connection-заголовки) (224d521, f04767c)
- Task #215 — минимальная длина пароля (12) для активации инвайта: `MIN_PASSWORD_LENGTH` в `auth.ts`, серверная проверка в `accept-invite.ts` до хеширования, `minLength` на форме инвайта, тесты на короткий/граничный пароль (commit TBD)

## Performance

### SigNoz (observability)

**Период:** 24ч (2026-08-11 06:00 → 2026-08-12 06:00 UTC)

| Metric | Value | vs 2026-08-11 |
|---|---|---|
| Запросов | 78375 | 45445 (+72%) |
| Ошибок | 41 (0.05%) | 16 (0.04%) |
| Медленные DB-запросы (>100мс) | 0 — все ≤48мс | 0 — все ≤45мс |

**Медленные точки (p99), с контекстом:**

- `GET /api/mcp/[apiKey]` p99 ~90мин — не регрессия: держащееся SSE-соединение с keep-alive пингами каждые 30с (`route.ts`).
- `mcp.tool threads_publish_thread` p99 14.0с — ожидаемо, укладывается в бюджет polling'а 25с.
- `mcp.tool searchConsole_inspect_url` p99 5.1с / `service-fetch searchConsole` p99 5.0с — тот же паттерн, что вчера → **H-001** теперь 2/3.
- `mcp.tool linkedin_create_post` p99 3.4с, лог-семпл "LinkedIn API timed out (>1250ms)" — единичный случай, ниже порога.

**Ошибки по маршрутам:** `/api/mcp/[apiKey]` 400×582 (0.74% трафика) — рост с 52 (0.11%) вчера, но тот же паттерн клиентских ошибок валидации → **H-002** теперь 2/3. Остальное — единичные сканеры (`/login.php` 404×1).

**Интеграции:** соединение Google Ads (`cmmxnepr200f90kpiy9hx5kxq`) — revoked, 3 инструмента подряд падают с "reconnect required"; уведомление о реконнекте уже отправлено системой (`notifyConnectionRevoked`) — не задача для агента, требуется ручной реконнект OAuth владельцем проекта.

Задач не заведено — обе гипотезы на 2/3 (нужен третий прогон), инцидентов нет (error rate 0.05%), рост трафика (+72%) не подтверждён как аномалия (данных за предыдущие дни нет для сравнения).
