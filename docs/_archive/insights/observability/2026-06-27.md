# Observability Review — 2026-06-27

**Tool:** SigNoz (self-hosted, signoz.chatindex.app)
**Period:** последние 24 часа | Сервис: `scopegate`

## Key Metrics

| Metric | Value | vs 2026-06-26 |
|---|---|---|
| Всего запросов | 9 077 | ↓ 13 043 → 9 077 (−30%, суббота) |
| Ошибок (has_error=true) | **0** | ✅ без изменений |
| Error rate | **0%** | ✅ 4-й день подряд |
| p99 SSE keep-alive (GET /api/mcp/[apiKey]) | 3 474 714 мс¹ | ≈ (ожидаемо) |
| p99 GET (unnamed orphaned span) | 1 006 208 мс¹ | ≈ (артефакт OTel) |
| p99 refresh_connection | 293.9 мс | впервые в топ (возможно, выше активность cron) |
| p99 oauth.fetch_token | 293.7 мс | — |
| p99 POST /api/cron/refresh-tokens | 293.6 мс | — |
| p99 mcp.tool openRouter_get_credits | 247.8 мс | впервые в топ |
| p99 service-fetch openRouter | 221.9 мс | — |
| Медленных DB-запросов (>100 мс) | **0** | ✅ без изменений |
| Макс. p99 DB (ServiceConnection UPDATE) | 23.0 мс | ✅ |

¹ SSE-стриминг держит соединение открытым часами — ожидаемое поведение; orphaned span — артефакт инструментации Next.js/middleware.

## Findings

### Ошибки

- **0 ошибок** ✅ — четвёртый день подряд с нулевым error rate. Система стабильна.
- HTTP-ошибки: `/[blocked-action]` 400 × 2 (middleware блокирует, норма), `/login.action` 404 × 1 (бот-сканнер), `/` 400 × 2 (внешние). Всё ожидаемо.
- Error log samples: пусто ✅

### Латентность

- **threads_publish_thread и linkedin_create_post** не вошли в топ-10 по p99 сегодня — либо операции не выполнялись (суббота), либо время снизилось ниже 220 мс. Позитивный сигнал.
- **refresh_connection / oauth.fetch_token** впервые в топ (~293 мс) — cron-рефреш токенов активен. Время приемлемо.
- **openRouter_get_credits** p99 = 247.8 мс — впервые видим openRouter в топ. Время хорошее (< 500 мс), наблюдать динамику.
- **GET (orphaned span)** p99 ~16 мин — стабильный артефакт, не растёт.

### База данных

Все запросы значительно ниже порога 50 мс:

| Запрос | p99 |
|---|---|
| UPDATE ServiceConnection (token update) | 23.0 мс |
| SELECT McpEndpoint | 19.0 мс |
| INSERT rate_limit_bucket (upsert) | 9.3 мс |
| SELECT EndpointPermission | 8.6 мс |
| SELECT ServiceConnection | 8.6 мс |

### Объём запросов

9 077 спанов за 24 ч vs 13 043 вчера (−30%). 7-дневный итог: 122 654 запросов, 15 ошибок (0.01%). Снижение объясняется субботой (пониженная активность) — норма.

### Интеграции

Трейсы присутствуют: cron-рефреш, openRouter, Prisma DB. Threads/LinkedIn сегодня не активны.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| threads_publish_thread p99 > 8 с | — | Вышел из топ-10, финальная оценка откладывается |
| GET (orphaned span) p99 ~16 мин | 3+ | Стабильный артефакт OTel; не растёт; низкий приоритет |

## Hypotheses

- openRouter появился в топ-10 латентности впервые — стоит отследить в следующих снапшотах. Если p99 превысит 1 с → создать issue.
- Снижение объёма на 30% в субботу ожидаемо; если в понедельник объём не восстановится до ~12–14k — изучить причину.

## Recommendations

1. **Мониторинг openRouter** — p99 247 мс приемлемо, но впервые в топе; задокументировать базовую линию и наблюдать тренд в понедельник.
2. **Orphaned span `GET`** — если начнёт расти (> 2 М мс p99) — завести issue на аудит OTel middleware-инструментации в Next.js.
3. **Threads/LinkedIn** — не активны сегодня; убедиться, что трейсы вернутся в понедельник, чтобы подтвердить финальное снижение p99.

## Новые GitHub Issues

Не созданы — все метрики в норме, новых проблем не выявлено.
