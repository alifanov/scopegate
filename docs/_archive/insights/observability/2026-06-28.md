# Observability Review — 2026-06-28

**Tool:** SigNoz (self-hosted)
**Period:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs вчера (2026-06-27) |
|---|---|---|
| Error rate | 0 ошибок (0.0%) | ✅ без изменений |
| p99 латентность (топ, без SSE) | 275 мс (refresh_connection) | ~стабильно |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ без изменений |
| Объём запросов (спанов) | 15 370 | +69% vs вчера (9 077) |

## Findings

### Ошибки

Ошибок не обнаружено ✅. HTTP 400 на `/[blocked-action]` — 2 события (middleware-заглушка), не являются ошибками приложения.

### Латентность

- **`GET /api/mcp/[apiKey]`** — p99 4 278 478 мс (~71 мин): ожидаемый артефакт SSE keep-alive соединений; не является проблемой.
- **`GET` (orphaned span)** — p99 1 814 497 мс (~30 мин): OTel-артефакт без атрибута `http.route`; значение выросло с ~16 до ~30 мин, но это коррелирует с ростом объёма в понедельник — дополнительных спанов накопилось больше. Стабильный паттерн, не требует действий.
- **`refresh_connection`** — p99 275 мс: приемлемо, стабильно.
- **`oauth.fetch_token`** — p99 275 мс: включает openRouter и другие провайдеры, в норме.
- **`POST /api/cron/refresh-tokens`** — p99 261 мс: батч-операция, приемлемо.

### База данных

Все запросы в норме:
- `ServiceConnection UPDATE` — p99 36.5 мс ✅
- `rate_limit_bucket INSERT ON CONFLICT` — p99 18.6 мс ✅
- `McpEndpoint SELECT` — p99 13.4 мс ✅
- `ServiceConnection SELECT` — p99 10.1 мс ✅
- `EndpointPermission SELECT` — p99 9.6 мс ✅

### Объём запросов

15 370 спанов за 24 ч vs 9 077 вчера (+69%). Рост ожидаем: вчера была суббота (пониженная активность), сегодня воскресенье с активностью в первой половине дня. 7-дневный итог в норме.

### Интеграции

Трейсы присутствуют: MCP SSE, cron-рефреш (Twitter/X OAuth, openRouter), Prisma DB. Threads и LinkedIn сегодня не активны (воскресенье) — ожидаемо.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| GET (orphaned span) p99 растёт | 4+ | Артефакт OTel; коррелирует с объёмом; не растёт независимо; низкий приоритет |
| 0 ошибок | 7+ дней подряд | ✅ Стабильно |

## Hypotheses

- **openRouter латентность** — oauth.fetch_token p99 275 мс стабильна вторую неделю; базовая линия зафиксирована. Если превысит 500 мс → создать issue.
- **Orphaned `GET` span** — p99 выросло с 16 до 30 мин, но коррелирует с объёмом (+69%). Если в следующем снапшоте p99 > 2 М мс при том же объёме — завести issue на аудит OTel middleware.

## Recommendations

1. **Без действий** — все производственные метрики в норме, ошибок нет, DB здорова.
2. **Мониторинг orphaned span** — отслеживать в следующих снапшотах; если p99 > 2 М мс при нормальном объёме — аудит OTel middleware.
3. **Threads/LinkedIn** — проверить наличие трейсов в понедельник для подтверждения нормальной работы.

## Новые GitHub Issues

Не созданы — все метрики в норме, новых проблем не выявлено.
