# 2026-09-06

## Security

**Period:** 2026-08-30 — 2026-09-06 (с прошлого прогона security-audit)

### Findings

| Category | Finding | Severity | File / Config | Source |
|---|---|---|---|---|
| Vulnerable dependency | `fast-uri` 4.1.2 — 4 GHSA (host confusion / парсинг authority); override уже стоит на `>=4.1.2` и попадает в уязвимый диапазон | high | `package.json` → `pnpm.overrides` | github |
| Vulnerable dependency | `mysql2` 3.15.3 — GHSA-3f6p-5ww8-9rcr + GHSA-rgwj-5xj2-c3m3; тянется peer'ом от `prisma`, в коде не используется (проект на PostgreSQL) | high | `package.json` → `pnpm.overrides` | github |
| Vulnerable dependency | `qs` 6.15.2 — GHSA-x5fp-wj9c-mxmx; override `>=6.15.2` устарел | medium | `package.json` → `pnpm.overrides` | github |
| Vulnerable dependency | `@humanfs/node` <0.16.8 — GHSA-p498-v437-472g (symlink escape в `copyAll()`); dev-only, транзитив eslint | medium | `package.json` → `pnpm.overrides` | github |
| Vulnerable dependency (low, задача не заводилась) | `postcss-selector-parser` <7.1.3 — GHSA-w9m9-85wc-3x92 | low | `package.json` | github |
| Robustness (low, задача не заводилась) | `saveOAuthApp`: `input.clientId.trim()` до проверки типа → 500 на нестроковом теле; повтор находки прошлого прогона | low | `src/lib/oauth-app-credentials.ts:44` | code-review |
| Log hygiene (low, задача не заводилась) | На 400 от MCP-транспорта тело запроса пишется в лог (`body_preview`, 500 симв.) и уезжает в SigNoz — валидный tool call с секретом в аргументах может туда попасть | low | `src/app/api/mcp/[apiKey]/route.ts:253` | code-review |
| Robustness (low, задача не заводилась) | `rateLimitPerMinute` не валидируется при создании эндпоинта — отрицательное значение блокирует эндпоинт наглухо, огромное снимает лимит | low | `src/lib/endpoint-permissions.ts:96` | code-review |

Code scanning (404 «no analysis found») и secret scanning (выключен) на репозитории недоступны — с этих поверхностей находок нет.

Все 8 алёртов Dependabot сведены в одну задачу #312: фикс — одна правка блока `pnpm.overrides` и один `pnpm install`; четыре отдельные задачи означали бы четыре прогона CI на self-hosted раннере, у которого уже были проблемы с ёмкостью (#257, #258, #289).

## Changes

- Task #312 — подняты пины в `pnpm.overrides`: `fast-uri` `>=4.1.3`, `qs` `>=6.16.0`, добавлены `mysql2` `>=3.23.1` и `@humanfs/node` `>=0.16.8` (1f5bd9d)

### Recurring Issues

- **`fast-uri`, 2-й прогон подряд.** Задача #213 (`>=4.1.2`) закрыта 2026-07, пин с тех пор не двигали — вышли новые GHSA, и застывший override теперь сам держит уязвимую версию. Пины вида `>=X` в `pnpm.overrides` не самообновляются: `pnpm install` резолвит по lock-файлу.
- **`oauth-app-credentials.ts:44`, 2-й прогон подряд** (low, порог для задачи не пройден).

### Recommendations

1. **Устаревающие пины в `pnpm.overrides`** → поднять `fast-uri` до `>=4.1.3`, `qs` до `>=6.16.0`, добавить `mysql2` и `@humanfs/node` → критерий: `pnpm why` не показывает версий из уязвимых диапазонов, 8 алёртов Dependabot закрываются после пуша. Задача #312.

### Проверено, чисто

Ревью кода за период (13 коммитов: разбор `ServicesTab` на компоненты, фикс BYO-кредов в OAuth start-роуте) находок уровня medium+ не дало. Перепроверены и держатся: BYO-креды теперь резолвятся через `resolveOAuthApp()` в `oauth-start-route.ts` (#288 закрыт, покрыт тестом); IDOR при создании эндпоинта закрыт `requireProjectServiceConnection()`; SSRF в IMAP/SMTP — allowlist портов + пиннинг IP в `mail-safety.ts`; CSRF-cookie + HMAC-состояние в OAuth-колбэке; deny-by-default в `getToolsByActions()` (фильтр и по action, и по провайдеру); гонка при приёме инвайта закрыта условным `updateMany`. Весь `dangerouslySetInnerHTML` — JSON-LD из статического контента, пользовательского ввода в нём нет.
