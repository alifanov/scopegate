# Observability Review — 2026-07-18

**Tool:** SigNoz (self-hosted, `signoz.chatindex.app`)
**Период:** последние 24 ч

## Key Metrics

| Метрика | Значение | vs 2026-07-17 |
|---|---|---|
| Error rate | 23 ошибки (0.05%) | 🟡 ↑ было 6 (0.01%) — рост за счёт `googleAds adGroupAds:mutate` 400 (см. Findings), в целом всё ещё низкий уровень |
| p99 латентность (топ, без SSE) | 11 101.6 мс (`threads_publish_thread`) | ✅ ↓ было 24 039.1 мс (−53.8%) — тренд роста (3 снапшота подряд) развернулся после диагностики #173 |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ стабильно (макс. 71.7 мс) |
| Объём запросов (спанов) | 45 805 | ✅ ↓ было 52 235 (−12.3%), но +11.2% к 7-дневному среднему (41 183/сутки) — в пределах нормальной вариации |

## Findings

### Ошибки

| Маршрут | Статус | Кол-во | Комментарий |
|---|---|---|---|
| `googleAds/customers/.../adGroupAds:mutate` | 400 | 4 | Новый паттерн, не встречался в прошлых снапшотах. Похоже на application-level валидацию Google Ads API (невалидные параметры объявления), не код-баг MCP-сервера |
| `POST` (агрегировано) | 400 | 4 | Вероятно тот же вызов, посчитанный на другом уровне спана |
| `GET` (root) | 404 | 1 | Единично, ниже порога |

Логов уровня ERROR/FATAL за период нет.

### Латентность

| Эндпоинт | p99 | Оценка |
|---|---|---|
| `GET /api/mcp/[apiKey]` | ~117.9 мин | ✅ SSE keep-alive — ожидаемо, стабильно vs 115.2 мин вчера |
| `mcp.tool threads_publish_thread` | 11 101.6 мс | ✅ Тренд роста развернулся (24 039.1 → 11 101.6 мс), см. Recurring Issues |
| `service-fetch threads` | 5 559.8 мс | ✅ Синхронно со снижением publish_thread |
| `mcp.tool searchConsole_inspect_url` | 5 053.9 мс | 🟢 Стабильно, известное поведение (TimeoutError в safe-fetch.ts), без изменений с 07-14 |
| `service-fetch searchConsole` | 5 027.9 мс | 🟢 |
| `mcp.tool linkedin_create_post` | 1 398.5 мс | 🟢 |
| `mcp.tool googleAds_create_ad` | 1 083.3 мс | 🟢 |

**Orphaned `GET` span — полностью исчез из топ-10.** Подтверждает прогноз из снапшота 07-17: долгоживущие SSE-соединения, открытые до фикса `54e7719`, полностью переподключились, старое имя `GET` больше не встречается.

### База данных

Все запросы в топ-5 < 75 мс — здоровое состояние, без изменений.

| Запрос | p99 | Оценка |
|---|---|---|
| `SELECT ServiceConnection` | 71.7 мс | ✅ |
| `UPDATE ServiceConnection` | 34.9 мс | ✅ |
| `SELECT pg_advisory_xact_lock(...)` | 29.5 мс | ✅ (ожидаемая сериализация `withConnectionLock`) |
| `SELECT McpEndpoint` | 14.8 мс | ✅ |

### Объём запросов

45 805 спанов за 24 ч, −12.3% vs вчера (52 235), но +11.2% к 7-дневному среднему (288 283 / 7 = 41 183/сутки) — обычная суточная вариация, без аномалий.

### Интеграции

Threads, Google Ads, LinkedIn, GSC — репортируют спаны стабильно. Пробелов в трейсинге не обнаружено.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| `threads_publish_thread` p99 | 07-15: 7 256 → 07-16: 11 770.7 → 07-17: 24 039.1 → 07-18: 11 101.6 мс | ✅ Разворот тренда после задачи #173 — root cause (retry/latency trade-off) подтверждён, деградации Meta API не выявлено |
| Orphaned `GET` span | затухал 07-16→07-17, сегодня отсутствует в топ-10 | ✅ Полностью устранено, фикс `54e7719` подтверждён |

## Hypotheses

- H: `googleAds adGroupAds:mutate` 400×4 — вероятно валидация на стороне Google Ads API (невалидные параметры объявления от вызывающей стороны), не код-баг. Ниже порога для задачи (0.05% error rate, 1 снапшот). Наблюдать в следующих 1-2 прогонах — если участится, завести задачу на улучшение сообщения об ошибке для этого tool.

## Recommendations

Значимых находок critical/high/medium нет — задачи не создаются.

1. Продолжать наблюдение за `googleAds adGroupAds:mutate` 400 — если паттерн повторится в 2+ снапшотах подряд, завести задачу на диагностику/улучшение error message.
2. `threads_publish_thread` и orphaned `GET` span можно считать закрытыми — оба подтверждены двумя снапшотами наблюдения после фикса.
