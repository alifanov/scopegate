## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-04 06:00 → 2026-09-05 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 33 071 | 36 537 |
| Ошибок (error rate) | 0 (0%) | 3 (0.01%) |
| p99 самого медленного эндпоинта | `GET /scopegate-demo.mp4` 723.3ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (держится часами, keep-alive каждые 30с), реальный «медленный» — `mcp.tool instagram_create_post` 20.5s |
| Медленные DB-запросы (>100мс) | 0 (макс. 14.4ms) | 1 — `McpEndpoint` lookup 109.3ms (ниже порога 200мс) |
| Запросов за 7 дней | 260 446 (~37.2к/сут) | 317 464 (~45.4к/сут) |
| Объём за 24ч vs 7-дн. среднее | −11.1% (в норме) | −19.4% (в норме, тот же диапазон, что 08-25/08-19) |

### Findings

- **[DB] `McpEndpoint` lookup** вернулась к норме — 109.3ms, ниже порога 200мс. H-007 (регрессия из 09-04, 326.9ms) снова разорвана чистым прогоном — тот же on/off паттерн, что и после блипа 09-01. Задачу не заводили.
- **Errors**: 3 ошибки на `scopegate-selfhost` — все от одного revoked-коннекта `searchConsole` (`cmlqlb01k000h0jlh8momegho`, `invalid_grant`, штатное поведение `classifyOAuthError`, уведомление о реконнекте уже отправлено). Не дефект.
- `mcp.tool instagram_create_post` (20.5s) / `threads_publish_thread` (19.6s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.
- Боевой (404/400/401) шум на обоих сервисах — сканирование ботами (`.git/config`, `wp-json`, `.well-known/*`) и попытки подбора MCP API-ключей на `scopegate-cloud` (448 x 401 на `/api/mcp/[apiKey]`); оба пути уже защищены (brute-force rate-limit в `api-keys.ts`, fail-open rate limiter).

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- H-007 — `McpEndpoint` lookup latency: streak снова разорвана чистым прогоном (109.3ms). Ледджер: `docs/state/hypotheses.md`.

### Recommendations

1. Продолжать наблюдение за `McpEndpoint` lookup — паттерн «вспышка → 2 чистых прогона» повторился дважды (09-01, 09-04); если разовые вспышки участятся, разбирать нагрузочный профиль отдельно от порога «3 прогона подряд».
