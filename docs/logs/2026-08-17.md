# 2026-08-17

## Performance

**Target:** https://scopegate.dev  ·  **Strategy:** mobile  ·  **Source:** lab (local Lighthouse, native arm64 node)

### Metrics
| Time | LCP | TBT | CLS | TTFB | FCP | Score | Result | Task |
|---|---|---|---|---|---|---|---|---|
| 06:03 | 2.6s | 76ms | 0.07 | 37ms | 1.2s | 96 | ok | — |

### Notes

Все метрики в зоне "good", кроме LCP — 2.6с чуть выше порога 2.5с (needs-improvement, не poor). `runWarnings` пуст, `vm.loadavg` 6.4/5.6/5.3 — умеренная нагрузка машины, не искажение как в задаче #198. TTFB 37мс подтверждает быстрый ответ сервера. Задача не заведена — ни одна метрика не в зоне "poor".

## SEO

GSC data skipped: Google Search Console MCP is not connected.
Connect the GSC MCP in your project's .claude/settings.json to enable it.

Аудит кода + live-проверка scopegate.dev: robots.txt/sitemap.xml корректны, canonical/title/h1 на всех хабах на месте, 404 отдаёт настоящий 404, OG/Twitter теги полные, `lastmod` в sitemap — реальные даты из git (не `new Date()`), внутренняя перелинковка хабов (`/compare`, `/integrations`, `/glossary`) на все дочерние страницы полная, орфанов нет.

Найдено: дублирование бренда в `<title>` — корневой шаблон `"%s | ScopeGate"` (`src/app/layout.tsx`) плюс страницы, которые сами дописывают `| ScopeGate`/`— ScopeGate` в свой title. Подтверждено live: `/blog`, `/glossary`, `/glossary/mcp-gateway`, 10 из 11 `/compare/*`, 10 из 11 `/integrations/*` — `<title>` заканчивается на `... | ScopeGate | ScopeGate`. → Task #255 (high, source:seo).

## Changes

- Task #255 — убрано дублирование «ScopeGate» в `<title>` на /blog, /glossary, /compare/*, /integrations/* (detail-страницы теперь используют `title: { absolute }`, обходя шаблон layout.tsx; список blog/glossary — убран ручной суффикс из строки)
- Task #233 — блок установки Prisma CLI в runner-стейдже `Dockerfile` перенесён выше нестабильных COPY-слоёв приложения, чтобы BuildKit кэшировал `npm install --prefix /prisma-runtime` между деплоями (содержимое образа не изменилось, только порядок слоёв)
- Task #237 — `docs/state/spec/flows/invite-onboarding.md` переписан: инвайт больше не назван единственным входом, добавлены облачные пути (Google, magic link) и причина редиректа через `/magic-link`; создан `docs/state/spec/flows/billing-checkout.md` (checkout Polar → webhook → лимиты в трёх точках)
- Task #238 — манифест `docs/README.md` приведён в соответствие с диском: добавлена строка `byo-credentials.md`; `overview.html` (устаревший, замещён `docs/logs/`) перенесён в `_archive/`; запись uptime 11:16 из воссозданной `docs/insights/uptime/2026-08-13.md` слита в архивный файл, каталог `docs/insights/` удалён (6881187)
- Task #239 — `docs/state/product/glossary.md`: строка про инвайт больше не противоречит облаку (Google-вход, magic link); добавлены термины «Облако / self-host», «План», «Месячная квота», «Собственное OAuth-приложение (BYO)», «Группа кредов»
