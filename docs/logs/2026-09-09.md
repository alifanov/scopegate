## Performance

**Tool:** SigNoz (`signoz.chatindex.app`)
**Period:** последние 24ч (2026-09-08 06:00 → 2026-09-09 06:00 UTC), два сервиса: `scopegate-cloud` (scopegate.dev) и `scopegate-selfhost` (chatindex.app)

### Key Metrics

| Metric | scopegate-cloud | scopegate-selfhost |
|---|---|---|
| Запросов | 37 414 | 42 468 |
| Ошибок (error rate) | 0 (0.00%) | 3 (0.01%) |
| p99 самого медленного эндпоинта | `NextNodeServer.clientComponentLoading` 156.5ms | `GET /api/mcp/[apiKey]` — SSE-артефакт (keep-alive держит соединение часами); реальный «медленный» — `mcp.tool instagram_create_post` 20.3s |
| Медленные DB-запросы (>100мс) | 0 (макс. 11.7ms) | 1 — `McpEndpoint` lookup 444.8ms |
| Объём за 24ч vs 7-дн. среднее | в норме (~36–38к/сут диапазон) | в норме (~40–47к/сут диапазон) |

### Findings

- **[DB] `McpEndpoint` lookup p99 444.8ms на `scopegate-selfhost`** — выше порога 200мс, выше всех прежних вспышек (H-006: 242.7/249ms, H-007: 259/326.9ms). H-007 закрыта только вчера (3 чистых прогона), сегодня — первое новое наблюдение после закрытия. Заведена **H-008** (1/3), задачу не создавал — ниже порога 3 прогонов/2 источников.
- 3 ошибки на `scopegate-selfhost` — 2× `youtube_upload_video` на revoked-коннекте (`invalid_grant`, уведомление о реконнекте уже отправлено, штатное поведение) + 1× `threads_publish_thread` timeout (>3500ms), retry-логика уже покрывает. Не дефект.
- `mcp.tool instagram_create_post` (20.3s) / `threads_publish_thread` (15.0s) — в рамках 25s-бюджета опроса контейнера Meta, задокументировано в CLAUDE.md; не новая находка.
- Боевой (400/401/404) шум на обоих сервисах — сканирование ботами (`.well-known/*`, `wp-admin/install.php`, `.env`, `secrets.json`) и подбор MCP API-ключей (396× 401 на `scopegate-cloud` `/api/mcp/[apiKey]`); уже задокументированный паттерн, оба пути защищены.

### Recurring Issues

Нет находок, дошедших до порога 3 прогонов/2 источников в этом снапшоте.

### Hypotheses

- **H-008** (новая) — `McpEndpoint` lookup DB-latency вспышка (444.8ms) после закрытия H-007; 1/3, `tracking`. Ледджер: `docs/state/hypotheses.md`.

### Recommendations

Нет рекомендаций уровня medium+ — оба сервиса в норме, единственная аномалия ниже порога заведения задачи.

## Changes

- Task #292 — `revokeProviderToken` теперь берёт Google/LinkedIn группу из `getCredentialGroup()` (provider-registry) вместо захардкоженного списка из 5 провайдеров; `youtube`/`googleTagManager` теперь тоже отзываются при отключении (104e55a)
- Task #302 — плавающая mailto-кнопка скрыта на `<md` (`hidden md:flex`), на мобильных тот же адрес — текстовая ссылка в футере (`md:hidden`), структурно вне колонки контента
- Task #303 — LCP на `/` (было 8с): убран JS-тайпрайтер hero-кодблока (`setInterval` + `invisible`-класс, рисовал строки только после гидратации); блок теперь рендерится целиком на сервере, `hero.tsx` стал обычным Server Component (снята `"use client"` — хуков больше нет)
