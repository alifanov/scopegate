# Observability Review — 2026-07-14

**Tool:** SigNoz (self-hosted, `signoz.chatindex.app`)
**Период:** последние 24 ч

> ⚠️ Разрыв в снапшотах: предыдущий отчёт — 2026-07-01 (13 дней назад). Рутина явно не запускалась ежедневно в этот период — сравнение «vs вчера» ниже сделано против 07-01, а не против предыдущего дня.

## Key Metrics

| Метрика | Значение | vs 2026-07-01 |
|---|---|---|
| Error rate | 6 ошибок (0.01%) | ↓ было 14 (0.03%) |
| p99 латентность (топ, без SSE-эндпоинтов) | 9 501 мс (threads_publish_thread) | ↓ было 10 217 мс, в рамках 25s budget |
| Медленные DB-запросы (> 100 мс) | 0 | ✅ стабильно (было 0) |
| Объём запросов (спанов) | 46 236 | ≈ стабильно (было 47 361) |

## Findings

### Ошибки

| Маршрут | Статус | Кол-во | Комментарий |
|---|---|---|---|
| `GET` (внешний) | 400 | 1 | Несущественно |
| `/` | 404 | 2 | Несущественно, вероятно боты |
| `/_next/image` | 400 | 1 | Вероятно, некорректный внешний URL изображения |

Логов уровня ERROR/FATAL за период нет. Все ошибки единичные, ниже порога для создания issue.

### Латентность

| Эндпоинт | p99 | Оценка |
|---|---|---|
| `GET /api/mcp/[apiKey]` | ~2.3 ч | ✅ SSE keep-alive — ожидаемо |
| `GET` (orphaned span) | ~62.9 мин | 🟡 Резкий рост vs 72 449 мс (~72 с) на 07-01 — но сравнение через 13-дневный разрыв ненадёжно, см. Hypotheses |
| `mcp.tool threads_publish_thread` | 9 501 мс | 🟢 Улучшение vs 10 217 мс, в рамках 25s budget |
| `mcp.tool searchConsole_inspect_url` | 5 063 мс | 🟡 Новый в топ-5, следить |
| `service-fetch threads` | 4 415 мс | 🟢 Ожидаемо для Threads async API |
| `mcp.tool linkedin_create_post` | 1 633 мс | 🟢 Стабильно |
| `linkedin_create_post.prepare_image` | 991 мс | 🟢 |
| `mcp.tool calendar_create_event` | 794 мс | 🟢 |
| `mcp.tool googleAds_get_ad_group_performance` | 729 мс | 🟢 |

### База данных

Все запросы в топ-5 < 25 мс — здоровое состояние.

| Запрос | p99 | Оценка |
|---|---|---|
| `UPDATE ServiceConnection` | 23.3 мс | ✅ |
| `SELECT session` | 16.1 мс | ✅ |
| `SELECT AuditLog` | 16.1 мс | ✅ |
| `SELECT McpEndpoint` | 14.9 мс | ✅ |
| `INSERT rate_limit_bucket ON CONFLICT` | 13.3 мс | ✅ |

### Объём запросов

46 236 спанов за 24 ч — практически без изменений относительно 07-01 (47 361). Uptime-проверка сегодня в 12:24 — сайт отвечает 200 за 0.3с.

### Интеграции

Threads, LinkedIn, Google Calendar, Google Ads, GSC — все репортируют спаны стабильно. Пробелов в трейсинге не обнаружено.

## Recurring Issues

| Issue | Снапшотов подряд | Статус |
|---|---|---|
| `GET` (orphaned span, SSE-артефакт) | Ранее 7+, но с разрывом в 13 дней | Требует переподтверждения — величина выросла на порядок, недостаточно данных для вывода о тренде |

## Hypotheses

- **`GET` orphaned span p99 вырос с ~72с до ~63 мин.** Возможно, продолжение того же известного SSE-артефакта (объём трафика/длина keep-alive соединений могли вырасти), либо новая причина. Из-за 13-дневного разрыва в снапшотах нельзя различить постепенный рост от одного скачка. Status: 1/3 снапшотов после разрыва — не готово для issue. Следить в следующих 2 ежедневных прогонах; если подтвердится на 07-15 и 07-16 — завести issue на исследование причины.
- **`searchConsole_inspect_url` p99 5 063 мс** — новый в топ-5 медленных, в рамках известного поведения (TimeoutError catch в safe-fetch.ts), но раньше не попадал в топ. Status: 1/3 снапшотов — наблюдать.

## Recommendations

1. Без новых issue — ни одна находка не достигла порога `medium`+ по критичности за один снапшот.
2. **Важно:** рутина `observability-check` не запускалась ежедневно с 07-01 по 07-14 (13 дней без снапшотов) — проверить, почему запланированный прогон (Daily 8:30) не срабатывал; без регулярных снапшотов правило «3 снапшота подряд» для recurring issues не работает.
3. Продолжить наблюдение за orphaned `GET` span и `searchConsole_inspect_url` в следующих 1-2 ежедневных прогонах.

## Новые GitHub Issues

Нет — ни одна находка не достигла порога `medium`+ по критичности.
